/**
 * LGDL SVG renderer — placeholder for v0.1.
 *
 * Takes a LayoutResult and produces SVG markup.
 */
export function renderSvg(): string {
  return '<svg xmlns="http://www.w3.org/2000/svg" width="0" height="0"></svg>';
}
