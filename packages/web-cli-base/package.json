{
  "name": "@lgdl/web-cli-base",
  "version": "0.5.0",
  "description": "AI-callable command execution framework — command registry, execution pipeline, tools schema, protocol parser and self-documenting help (domain-neutral, zero LGDL dependency)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc src/*.test.ts --outDir dist-test --module nodenext --moduleResolution nodenext --target es2022 --esModuleInterop --skipLibCheck --declaration false --allowImportingTsExtensions --rewriteRelativeImportExtensions && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "openai": "^7.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
