# Feature Specification：specs-tree-v55f-1-ref-context-and-anchor（V5.5F-1 范围底座：引用事实进回合 + 范围读数 + ref 锚定）

> **文档定位**: SDDU 需求规范（**叶级切片**）— 父规范 `../spec.md`（v1.0）在**本叶**的适用范围与承载条文；作为本叶 plan 阶段的输入
> **前置依赖**: 父 `../discovery.md`（v1.0）+ 父 `../spec.md`（v1.0）§5.1~5.4 / §5.7~5.12 / §9 / §12 / §13 / §14.3
> **创建人**: SDDU Spec Agent
> **创建时间**: 2026-09-23
> **版本**: v1.0
> **更新人**: SDDU Spec Agent
> **更新时间**: 2026-09-23
> **更新说明**: 初始创建（V5.5F-1 范围底座叶规范：引用事实进回合 + 系统段组装 + **范围读数单源（法九）** + `--ref` 锚定（仅写命令）+ 失配 fail-closed + 留痕扩字段 + **S0′ 双向样板**）

---

## 1. 元数据

| 字段 | 值 |
|------|-----|
| Feature ID | `specs-tree-v55f-1-ref-context-and-anchor`（父 = `specs-tree-web-cli-plugin-v55-f-scope-governance`，ROADMAP **F-34** / **v0.11.1**） |
| 名称 | **V5.5F-1 范围底座**：引用事实进回合（`chat` type-only 载荷 + 系统段基座 + 追加段）+ **范围读数单源（法九）** + 工具面 `--ref <n>` 锚定（**仅 `dom set-text`**；plugin 侧包装；单节点保证；失配 / 失效 fail-closed 非静默）+ 留痕扩字段 + **S0′ 双向样板（含反证）** |
| 优先级 | P0（本叶 = **底座叶 / 首叶**，叶2 依赖本叶） |
| 目标版本 | v0.11.1（承父；**每叶收口实测重登记**，父 FR-SGO-125） |
| 交付顺序 | **1 / 2**（首叶）；`dependsOn: []` |
| 深度 / 类型 | depth=2；`leaf: true` |
| 承载父 FR | **GOV 001~006** · **REFCTX 010~019** · **SCOPE 020~028** · **ANCHOR 030~038** · **LAW9 070~077** · **TRACE 080 / 083 / 084** · **S0′ 090 / 091 / 092 / 094** · **SUPERSEDE 100 / 101 / 102 / 104 / 106** · **GATE 110~116** · **VOL 120~125**（共 **≈62 条父 FR 切片**） |
| 相关干系人 | 作者（唯一真实用户 + 唯一决策者）；编排器；下游 @sddu-plan / @sddu-tasks / @sddu-build / @sddu-review / @sddu-validate（**本叶承接 build/review/validate**） |

---

## 2. 上下文与边界

### 2.1 上下文

父规范 §2.1~§2.4 的**根因 A/B/C/D/E/F/H/I/J/K/L/N/O/P/Q/R** 全部落在本叶：回合载荷只有 `user`（`sidepanel.ts:329`）、系统段静态常量（`service-worker.ts:107-118`）、base 工具零 ref（`packages/web-cli-base/src/dom-tools.ts:462-470`）、锚点已存在（`content/ref-capture.ts:67,370`）、面板 / SW 断层（`service-worker.ts:951`）、包装先例（`src/tools/chrome-host.ts:206`）、引用事实结构现成（`l1/ref-validity.ts:116-129` / `l1/ref-store.ts:140-142`）、留痕零值（`next-registry/ai-drive.ts:85`）、调用点计数（`test/op-wiring.test.ts:128-133`）、载体冻结（`messaging.ts:103-141` / `stream-model.ts:51-73` / `host-registry.ts:105`）、base 零 diff（`test/insight-no-escalation.test.ts:147`）、法七扩展形态（`test/law7x-ext.test.ts`）、法八四面（`test/ui/law8-plaintext.mjs:56-61`）、体积三值（`test/size-baseline.ts:351,506,522`）。

**决定性反证（本叶的验收锚）**：`ty.md:545-551` AI 已读到 `data-wcli-ref="ref_1"`、`ty.md:563-567` 已证 `[data-wcli-ref]` 恰 1 命中，**仍未锚定** ⇒ 本叶交付的正是**让这条信息到达 + 让范围可判 + 让写动作可锚定**。

### 2.2 范围（做 / 不做）

| 做 | 依据 |
|---|---|
| `chat` 载荷 **type-only** 引用事实字段（字段集 = `refNum` / `refId` / `selector` / `refMark` / `textDigest` / `refState` / `nodeCount`） | 父 FR-SGO-010 / 011 |
| **口径写死**：页面文本可入 LLM 上下文 / 凭据值不可 / 留痕只含字段名 | 父 FR-SGO-012 / N-SGO-026 |
| 两条回合入口**同一构建点**（`requestTurn` 内）；`requestTurn(` 仍**恰 2** | 父 FR-SGO-013 / 014 / N-SGO-014 |
| SW 侧 `runChat` 收结构化载荷；系统段 = **基座 + 追加段**；无引用回合**逐字不变** | 父 FR-SGO-015 / 016 / 017 / N-SGO-029 |
| **范围读数单源**（`in-scope` / `out-of-scope-authorized` / `out-of-scope-unauthorized` / `no-ref`）+ **法九机核门禁**（双向反证 + 注入必红 + 三段控制禁恒真 + 真源切片） | 父 FR-SGO-021~025 / 070~077 / N-SGO-024 / 028 |
| `--ref <n>` → `[data-wcli-ref="ref_n"]`（**仅 `dom set-text`**）；plugin 侧包装；单节点保证；失配 / 失效 fail-closed 非静默 | 父 FR-SGO-030~037 / N-SGO-027 |
| 留痕扩**范围读数**字段名（不含值）+ 扩围事实可读 | 父 FR-SGO-080 / 083 / 084 |
| **S0′ 全链机器化**（S0′-1~10）+ **双向反证**（去注入 ⇒ 读数 `no-ref` 必红） | 父 FR-SGO-090~092 / 094 |
| X-SGO-1 / 2 / 3 / 5 / 7 的**等价重锚**（X-SGO-7 = 未发生取代，如实登记） | 父 FR-SGO-100 / 101 / 102 / 104 / 106 / 107 |
| 门禁等价重锚 + 台账 + 体积分列预算逐叶重登记 | 父 FR-SGO-110~116 / 120~125 |

| 不做 | 依据 |
|---|---|
| 任务级批量授权（计划 / 指纹 / 一次手势 / 计划外回落 / 批量变体注入必红） | 父 §14.2 → **叶2**（`specs-tree-v55f-2-batch-consent`） |
| WIDEN 二择的**批量侧**接线（扩围读数转值本叶交付，二择卡在叶2 与批量计划耦合处落地） | 父 FR-SGO-060~063 → 叶2（本叶仅交付读数与转值基础 FR-SGO-021 / 026） |
| 读命令 `--ref`（`read-element` / `structure`） | 父 NG-SGO-010 / PD-SGO-001 |
| 每回合只读重观测 | 父 NG-SGO-013 / PD-SGO-002 |
| base / content / pick-layer / 判定链 / v5.5 / R6 产物 | 父 NG-SGO-002~005 / 011 |

### 2.3 与父规范的关系

本叶是父规范的**实施切片**：FR 编号**沿用父编号**（不另造叶内 FR 编号；叶内仅用 `LG-V55F-1-###` 标目标）。**父 FR 在本叶的承载切片 = §4 表**；本叶不引入父规范之外的需求；父 §11 的 DC-SGO-001~009 全部适用。

---

## 3. 目标与非目标（本叶）

### 3.1 目标

| # | 目标 | 父 FR |
|---|---|---|
| **LG-V55F-1-001** | 回合（两条入口）**携带引用事实**，使「用户指的是什么」**可判命中** | FR-SGO-010~015 |
| **LG-V55F-1-002** | **范围读数单源**成立且**可机核**（法九）+ 提示词仅作引导 | FR-SGO-020~028 / 070~077 |
| **LG-V55F-1-003** | `set-text` 可按 `--ref <n>` 锚定，**单节点保证** + 失配 **fail-closed 非静默** | FR-SGO-030~037 |
| **LG-V55F-1-004** | **S0′ 双向样板**（含「去注入 ⇒ 读数为空必红」反证）可机核 | FR-SGO-090~092 |
| **LG-V55F-1-005** | 冻结面零触碰 + base 零 diff + **体积逐叶重登记** | FR-SGO-120~125 / N-SGO-001~007 |

### 3.2 非目标

| # | 不做 | 父依据 |
|---|---|---|
| **LNG-V55F-1-001** | 批量授权（叶2） | §14.2 |
| **LNG-V55F-1-002** | 读命令 `--ref` | NG-SGO-010 |
| **LNG-V55F-1-003** | 新增终态字面量（**优先以读数承载**；扩张须 plan 显式裁决） | FR-SGO-105 / DC-SGO-007 |
| **LNG-V55F-1-004** | 新驱动时机 / 驱动者 / 调用点 | FR-SGO-006 / 028 / N-SGO-014 |
| **LNG-V55F-1-005** | 改 base / content / pick-layer / 判定链 / v5.5 / R6 产物 | N-SGO-002~008 / 020 |

---

## 4. 功能需求（本叶承载的父 FR）

| 父 FR | 叶内要点（切片口径） | 优先级 |
|---|---|:--:|
| **FR-SGO-001~005** | 父级结构 / 纪律 / 红线承载 / X 映射（**共享面在本叶一次做完**：体积 / 保护段 / 取代台账 / `knownGap`） | P0 |
| **FR-SGO-006** | 主流程零扩张（`requestTurn(` 2 / `maybeRecommend(` 7 / `nextAfterSettle` 单入口） | P0 |
| **FR-SGO-010** | `chat` 载荷 type-only 引用字段；`KIND_SET` 40 不动 | P0 |
| **FR-SGO-011** | 字段集 7 项（`refNum` / `refId` / `selector` / `refMark` / `textDigest` / `refState` / `nodeCount`） | P0 |
| **FR-SGO-012** | 口径写死（页面文本可入上下文 / 凭据值不可 / 留痕只含字段名） | P0 |
| **FR-SGO-013 / 014** | 两条入口同一构建点（`requestTurn` 内）；不新增调用点 | P0 |
| **FR-SGO-015 / 016 / 017** | SW 收结构化载荷；基座 + 追加段；无引用零漂移 | P0 |
| **FR-SGO-018** | 只取 `valid` 引用；`invalid` / `unknown` 不入范围 | P0 |
| **FR-SGO-019** | 断层显式化：SW 的引用事实唯一来源 = 回合载荷 | P0 |
| **FR-SGO-020~023** | 法则条文 + 双轨关系（读数 = 判据 / 提示词 = 引导） | P0 |
| **FR-SGO-024** | 读数单源（新模块，如 `l1/ref-scope.ts`）；第二声明 ⇒ FAIL | P0 |
| **FR-SGO-025~027** | 越界写被拦；扩围只由用户确认产生；「引用在但非意图」出口（读数侧） | P0 |
| **FR-SGO-028** | 法则不引入新时机 / 驱动者 | P0 |
| **FR-SGO-030** | `--ref <n>` → `[data-wcli-ref="ref_n"]`（**仅 `set-text`**） | P0 |
| **FR-SGO-031 / 032** | plugin 侧包装（`chrome-host.ts:206` 先例）；base 零 diff；risk 不放宽 | P0 |
| **FR-SGO-033 / 034** | 单节点保证（`nodeCount === 1`）；失配 fail-closed 非静默 | P0 |
| **FR-SGO-035 / 036 / 037** | 先落写命令；`--ref` 与 `--selector` 同给 ⇒ 显式错误；判定链零触碰 | P0 |
| **FR-SGO-038** | 失效可判（SPA 丢标记非静默；复用 R6 只读观测面；**不新增每回合探测**） | P1 |
| **FR-SGO-070~072** | 立法法九 + 判据形态（双向反证 / 注入必红 / 三段控制 / 真源切片） | P0 |
| **FR-SGO-073~076** | 四必判项（`no-ref` / `in-scope` / `out-of-scope-unauthorized` / `out-of-scope-authorized`） | P0 |
| **FR-SGO-077** | 门禁入受审集合；`CHROMIUM_GATES === 9` 不动 | P0 |
| **FR-SGO-080 / 083 / 084** | 留痕扩范围读数**字段名**；零值纪律；逐行可机核 | P0 |
| **FR-SGO-090 / 091 / 092 / 094** | S0′ 全链机器化 + 「改写处数 ≤ 引用数」+ **双向反证** + 人工面如实登记 | P0 |
| **FR-SGO-100 / 101 / 102 / 104 / 106** | X-SGO-1 / 2 / 3 / 5 / 7 等价重锚（X-SGO-7 = 未发生取代，如实登记） | P0 |
| **FR-SGO-107** | 未发生取代如实登记；台账与重锚同轮完成 | P0 |
| **FR-SGO-110~116** | 断言只增 / 反证实跑 / 台账 / 保护段 / 串行 / 新门禁登记 / 对账表 | P0（116 = P2） |
| **FR-SGO-120~125** | 分列预算 / 充分利用 B 列 / 距档结论 / 冻结面零容差 / 越限路径 / 逐叶重登记 | P0 |

---

## 5. 非功能需求（本叶相关）

| 父 NFR | 叶内口径 |
|---|---|
| **NFR-SGO-001** | `dist/sidepanel.js` ≤ 生效上限；判定 = 公式唯一（`record-only` cap） |
| **NFR-SGO-004** | 法八四面不退化（`law8` 36 零降级） |
| **NFR-SGO-005** | base 零 diff + 判定链零触碰 |
| **NFR-SGO-006** | 锚定 / 读数一律 fail-closed |
| **NFR-SGO-007** | 零新增载体（`KIND_SET` 40 / 12 kind / 零宿主） |
| **NFR-SGO-008** | 引用注入**零额外页面探测** |
| **NFR-SGO-009** | 无引用回合**逐字不变** |
| **NFR-SGO-010** | 法九门禁全绿（双向反证 + 禁恒真） |
| **NFR-SGO-011** | 留痕可判且零值 |
| **NFR-SGO-013** | 门禁串行 / 无新依赖 / 不改 `opencode.json` |
| **NFR-SGO-014** | 读数单源 + 唯一声明 |

> 本叶**不承载** NFR-SGO-002 / 003 / 012 的批量侧断言（属叶2）；但**不得**使其回退。

---

## 6. 边界情况（本叶相关）

| 父 EC | 叶内口径 |
|---|---|
| **EC-SGO-001** | 锚定 0 命中 ⇒ 非静默 + 指引；不回退 `--selector` |
| **EC-SGO-002** | 多命中 ⇒ 拒绝锚定；不按首元素 |
| **EC-SGO-003** | 标记缺失（best-effort）⇒ 非静默 + 指引 |
| **EC-SGO-004** | 引用失效 ⇒ 不入范围；不沿用旧选择器 |
| **EC-SGO-007** | 对话中途失效 ⇒ 非静默；不新增每回合探测 |
| **EC-SGO-008** | 无引用回合 ⇒ `no-ref` + 逐字不变 |
| **EC-SGO-015** | `--ref` 与 `--selector` 同给 ⇒ 显式错误 |
| **EC-SGO-016** | `--ref n` 越界 ⇒ fail-closed 非静默 |
| **EC-SGO-017** | `--ref` 用在读命令 ⇒ 不支持（schema 未声明） |
| **EC-SGO-018** | 页面文本注入法则 ⇒ 机制判据不受影响 |
| **EC-SGO-019** | 页面文本含凭据形值 ⇒ 不入 LLM 上下文（掩码） |
| **EC-SGO-020** | LLM 未配置 ⇒ 引用注入 / 读数**不得**绕过既有前置判据 |
| **EC-SGO-021** | `KL-N-10` flake ⇒ 隔离复跑 ≥2 + 如实记录 |
| **EC-SGO-022** | 体积越限 ⇒ 越生效上限重登记基线 / 越档位走 EC 显式路径 + 作者一行 |

---

## 7. 验收锚点（本叶 → 父 AC）

| 父 AC | 本叶判据 |
|---|---|
| **AC-SGO-001** | S0′ 全链（node + Chromium 双面；`test/ui/s0-self-driven.mjs` 只加断言不加文件） |
| **AC-SGO-002** | 引用事实进回合（两入口 + 可判命中） |
| **AC-SGO-003** | 法九四值可判 + 双向反证 + 禁恒真 |
| **AC-SGO-004** | X-SGO-1 / 2 / 3 / 5 / 7 等价重锚（含「未发生」登记） |
| **AC-SGO-006** | 零新增载体 |
| **AC-SGO-007** | 锚定 fail-closed 非静默（四类判定） |
| **AC-SGO-010** | 留痕含范围读数且零值 |
| **AC-SGO-011** | 无引用回合零漂移 |
| **AC-SGO-012** | 只取有效引用 |
| **AC-SGO-013** | 双向反证（去注入 ⇒ `no-ref` 必红） |
| **AC-SGO-014** | 完成交代如实（清单条数 == 实际改写处数） |
| **AC-SGO-017~022** | 门禁治理（只增 / 反证 / 台账 / 受审集合 / 串行 / 单节点） |
| **AC-SGO-023~025** | 冻结面零容差 / 分列预算 / base 零 diff |
| **AC-SGO-026** | 人工面如实登记（M1 / M4 属本叶；M2 / M3 属叶2） |

---

## 8. 交付与执行

### 8.1 上游依赖

- 父 `../spec.md`（v1.0）+ `../discovery.md`（v1.0）+ `../state.json`
- v5.5 三叶 `validated` 产物（驱动者层 / 悬置单源 / 法七扩展终态 / 三档清分 / 12 kind / 零宿主）
- R6 `74d76c1`（只读观测面 `reobserveAfterWrite` 可复用；其产物**零改写**）
- 仓库现状：`feature/web-cli-plugin`，本叶 plan 起点的 HEAD

### 8.2 下游 consumer

- **叶2** `specs-tree-v55f-2-batch-consent`（依赖本叶的**范围读数**与**锚定解析**底座）

### 8.3 交付物（本叶）

| # | 交付物 | 形态 |
|---|---|---|
| 1 | 引用事实载荷 + 系统段组装（B 列 `background.js` 为主） | 源码 + 断言 |
| 2 | 范围读数单源模块（A 列；如 `l1/ref-scope.ts`） | 源码 + 单测 |
| 3 | `--ref` 包装层（`src/tools/*`，B 列）+ 单节点 / 失配口径 | 源码 + 单测 + 反证 |
| 4 | **法九门禁** `test/law9-scope-reading.test.ts`（node） | 门禁 + 反证记录 |
| 5 | **S0′ 样板**（`test/ui/s0-self-driven.mjs` 增量 + node 面） | 门禁断言（**不新增 Chromium 门禁文件**） |
| 6 | 取代台账条目（X-SGO-1 / 2 / 3 / 5 已发生；X-SGO-7 未发生） | 台账 + 一致性 |
| 7 | 体积逐叶重登记（五要素 + V3-VOL-3 三值） | `test/size-baseline.ts` 登记条目 |
| 8 | 本叶 plan / tasks / build / review / validate 产物 | 各阶段产物（本叶承接） |

### 8.4 叶内执行序（供 plan / tasks 参考，**不是需求**）

> 承 discovery §6.3「**先定底座、再做批量授权**」与 DC-SGO-007「**先定读数再定法则**」。

1. **口径与载荷**（FR-SGO-010~014）：type-only 字段 + 唯一构建点 + 两入口。
2. **SW 侧组装**（FR-SGO-015~017）：结构化载荷 + 基座 + 追加段 + 零引用不变。
3. **读数单源**（FR-SGO-021 / 024）：四值 + 判定函数 + 唯一声明。
4. **法九门禁**（FR-SGO-070~077）：四必判项 + 三段控制 + 真源切片 + 反证。
5. **锚定包装**（FR-SGO-030~036）：`--ref` → `[data-wcli-ref="ref_n"]` + 单节点 + 失配口径。
6. **留痕**（FR-SGO-080 / 083 / 084）。
7. **S0′ 样板**（FR-SGO-090~092）+ 人工面登记（FR-SGO-094）。
8. **取代台账 + 门禁治理 + 体积重登记**（FR-SGO-110~125）。

---

## 9. 风险（本叶）

| # | 风险 | 等级 | 应对 |
|---|---|:--:|---|
| **R-SGO-002** | 范围漂移不可判 + 恒真断言 | 高 | 先定读数（执行序 3）再定法则（4）；四必判项各带反证 |
| **R-SGO-003** | base 零 diff 被撞 | 高 | 包装层（执行序 5）；`insight-no-escalation` 必绿 |
| **R-SGO-004** | 失配静默 ⇒ 写错节点 | 高 | FR-SGO-033 / 034 + EC-SGO-001~003 反证 |
| **R-SGO-006** | 体积越档 | 中高 | 先出分列预算；逐叶重登记（本叶先测） |
| **R-SGO-008** | 载荷扩张引发门禁重锚 | 中高 | 唯一构建点 + 调用点计数不变 |
| **R-SGO-901** | 读数与引用判定双源漂移 | 中高 | 读数只消费 `refState`，不重判 |
| **R-SGO-902** | type-only 被实现成新 kind | 高 | `KIND_SET` 长度 + `content.js` sha |
| **R-SGO-903** | 读数恒真 | 中高 | 三段控制 + 四必判项 |
| **R-SGO-904** | 包装层放宽 risk | 高 | 逐字段对照 base |
| **R-SGO-908** | 无引用回合漂移 | 中高 | 逐字 baseline |
| **R-SGO-909** | S0′ 脚本绿而非链路可判 | 中高 | 真源切片 + 双向反证 |
| **R-SGO-910** | 体积评估被跳过 | 中高 | 先预算后落地 |

---

## 10. 开放问题（本叶相关）

| # | 问题 | 状态 |
|---|---|---|
| **PD-SGO-001** | 读命令 `--ref` 是否同加（本叶**不做**） | 待裁决（后续轮 / 父 plan） |
| **PD-SGO-002** | 是否每回合注入当前只读重观测（本叶**不做**） | 待裁决 |
| **PD-SGO-003** | 引用表字段集是否扩 `semanticPath` / `origin` / `navSeq` | 待裁决（plan 评估体积） |
| **PD-SGO-004** | 范围读数是否入流内可见面（本叶只入留痕 + 门禁） | 待裁决（密度 / 12 kind 评估） |
| **PD-SGO-005** | 批量计划生成路径（属叶2 但影响本叶读数接口形态） | 待裁决（plan） |

---

## 修订记录

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| v1.0 | 初始创建（V5.5F-1 范围底座叶规范）：承载父 FR **≈62 条切片**（GOV / REFCTX / SCOPE / ANCHOR / LAW9 / TRACE 部分 / S0′ 部分 / SUPERSEDE 部分 / GATE / VOL）；交付 8 项；执行序 8 步（**先读数后法则**）；风险 12 条；开放问题 5 条 | 2026-09-23 | SDDU Spec Agent |
