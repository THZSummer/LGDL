# 验证报告：specs-tree-v4-3-ask-auth-inflow（V4-3 ask-user / 授权卡流内化与留痕固化）

> **文档定位**: SDDU 验证策略 — 指导 validate Agent 执行自主验证的场景和方法；验证结果见 validate-report.md
> **前置依赖**: 本叶 `spec.md` v1.0（FR-CHAT-040~049 / NFR-CHAT-001·004·005·009·010·012 / EC-CHAT-001·002·003·009·013 / AC-CHAT-003·005·014·015·016·021·023·025）、本叶 `plan.md` v1.0（ADR-V4-030~034）、本叶 `build.md` v3.0（review 修复轮）、`review.md` v1.0（C1~C13 策略）、`review-report.md` R1（BLOCK-01~04 / I-01~08，状态：**已修复待验证**）
> **创建人**: SDDU Validate Agent
> **创建时间**: 2026-09-19
> **版本**: v1.0
> **更新人**: SDDU Validate Agent
> **更新时间**: 2026-09-19
> **更新说明**: 初始创建（策略与报告同轮产出，用户指令「独立动态验证」）。定义 **V1~V9** 验证场景（门禁独立复跑 / 假留痕对抗 / 终态机遍历 / 答案真值 / 零明文 / 展开态预算 / R1 语义等价 / 规范红线 / 修复落地抽查）。

---

## 1. 验证概要

| 维度 | 实测数据 | 达标？ |
|------|---------|:--:|
| FR 测试覆盖 | 100%（10/10，FR-CHAT-040~049） | ✅ |
| NFR 测试覆盖 | 100%（6/6，NFR-CHAT-001/004/005/009/010/012） | ✅ |
| 构建 | typecheck + build 退出码 0（`dist/sidepanel.js` = 445,300 B） | ✅ |
| 接口一致性 | N/A（0 个外部服务 API；受管 Provider 0 调用） · 内部契约（DOM/事件）22 门禁 + 探针全绿 | ✅ |
| 漂移项 | 0 项（spec.md/plan.md 自立项零修改；孤立代码 0；需求缺失 0） | ✅ |
| 阻塞问题 | 0 项 | ✅ |

> **Feature 类型**：代码类 Feature（`src/ui/sidepanel/**` + `test/**` + `dist/`）⇒ 全维度验证（测试覆盖 / 接口数据 / 构建 / 性能边界 / 漂移检测）。
> **不动面声明**：本 Feature **0 个外部服务 API、0 次受管 Provider 调用**（路由门禁不涉及）；接口维度改判为「内部事件/DOM 契约一致性」（与父 spec 的零外部依赖一致）。

---

## 2. 自主验证场景（V1~V9）

**验证对象来源**：
- `spec.md`：FR-CHAT-040~049（10）/ NFR（6）/ EC（5）/ AC（8）→ 逐项验证实现完整性与验收标准
- `plan.md`：ADR-V4-030~034（事件化状态机 / 超时·取消·取代留痕 / 回合语义 / 授权回看 / 零明文）
- `build.md` v3.0：review 修复轮 BLOCK-01~04 + I-01~08 的落地声明
- `review-report.md` R1：4 个阻塞（假批准 / 假超时 / 记错答案 / 审计死链）→ **对抗性复现**
- `src/**` + `test/**` + `dist/`：源码、测试、真实产物

| # | 验证对象 | 验证步骤 | 预期结果 | 验证维度 | 验证方法 |
|---|---------|---------|---------|---------|---------|
| **V1** | 22 门禁 + RP-V4-09 与计数只增不减 | 严格串行重跑 22 项门禁；对账 build §10.6 登记计数 | 全 `exit=0`；npm 945 / ask-auth 61 / l1 111 / supersession 31；计数只增不减 | 构建 + 测试覆盖 | 自跑门禁（日志全量落盘） |
| **V2** | 假留痕对抗（审查抓过的 4 个洞） | ①auth 卡三路径 → 渲染「已取消（未授权，不执行）」②DOM 伪造 approved → 仲裁拒 ③回合结束对 ref-round-* → 无「提问超时未答」④审计入口点击 → 真实可见变化 | 四条全部「攻击失败」；绝不出现假「已批准」/假「超时」 | 接口数据 + 漂移 | 独立 node 探针 + Chromium 探针 |
| **V3** | 终态机全路径遍历 | 长度 4 × 10 操作字母表穷举（10000 序列）+ 定向竞态序；每步断言 open≤2 / 单终态 / 无伪造 approved / ref 不假超时 / seq 严格 | 无漏路径、无越界终态 | 测试覆盖 + 漂移 | 自写遍历脚本（真实 reducer） |
| **V4** | 答案真值 | 流内 choice/text 回答 / 改选同主题 / 取消 → L1「已决策历史」文案 = 卡终态 | 记录真实答案；改选标记；取消文案可读（非「（无回答）」） | 接口数据 | node 探针 + Chromium 探针 |
| **V5** | 零明文 | 注入敏感 marker（URL query / 密钥 / 命令参数体 / 原始标记）到答案/理由/输入 → 读回摘要与渲染面 | 摘要无自由文本字段；label 工厂拒收；渲染面零命中 | 接口数据 + 性能边界（安全） | node 探针（真实 `digestEntryOf`/`label`） |
| **V6** | 展开态预算（I-03） | Chromium 实测收起 4 / 展开 4 / 两卡展开合计 8；**自注入**「选项行不收起」 | 收起≤6、展开≤6、两卡≤8、展开时可见选项行=0；注入必红、还原必绿 | 性能边界 | Chromium 探针（自有测量函数） |
| **V7** | R1 语义等价全链 | AI 后台提问 → 拾取(supersededAsk) → 旧 ask canceled(superseded) 双留痕 → 新 ref-round 卡 → 回答 → AI 继续 | 全链可读、不掉的线、无假超时 | 测试覆盖 + 接口数据 | node ESM 全链驱动（真实 reducer） |
| **V8** | 规范符合 + 红线 | AC 逐条证据；红线零 diff；保护段；体积五要素抽查 | AC 全绿；红线 0 diff；445,300 五要素一致 | 漂移 + 构建 | git diff / sha256 / 台账读取 |
| **V9** | 修复落地抽查 | BLOCK-01~04 证据复核；I-01 label() 生产调用点；I-05 叶段登记抽 3 条 + 全量逐字比对 | 修复真实落地；登记逐字命中 | 漂移 | 源码走查 + python 独立复算 |

> **质量门槛**：每个 FR ≥ 1 个 Vx（见 §3.1 映射）；每个相关维度 ≥ 1 条（测试覆盖 V1/V3/V7 · 接口数据 V2/V4/V5/V7 · 构建 V1/V8 · 性能边界 V5/V6 · 漂移 V2/V3/V8/V9）。无法执行项显式标注（本叶无 N/A 场景）。

### 2.1 FR / NFR / EC → Vx 覆盖矩阵

| 需求 | 覆盖 Vx |
|------|--------|
| FR-CHAT-040（ask 迁入流内 / 独占槽移除） | V1 · V3 · V8 |
| FR-CHAT-041（操作前/后固化） | V2① · V4 · V6 |
| FR-CHAT-042（不可二次 / 取消留痕） | V2① · V2③ · V3 · V4 |
| FR-CHAT-043（60s 超时 → canceled 留痕） | V2③ · V3(E1) · V4 |
| FR-CHAT-044（superseded = 取消 + 留痕） | V2① · V3(E3/E4/E5) · V7 |
| FR-CHAT-045（confirm → auth 卡 / 批准拒绝固化） | V2① · V2④ · V4 |
| FR-CHAT-046（范围/后果预演 + 审计入口 + 不可重复 + 旧卡不消失） | V2② · V2④ · V5 |
| FR-CHAT-047（授权记录流内可回看 + 审计分工） | V2④ · V4 · V8(AC-016) |
| FR-CHAT-048（busy / 回合语义 / 无「永远处理中」） | V3(遍历) · V3(E7) · V7 |
| FR-CHAT-049（零明文边界） | V5 · V3(不变量) |
| NFR-CHAT-001（留痕完备性） | V2③ · V3 · V7 |
| NFR-CHAT-004（固化态无障碍） | V1(ask-auth ⑧) · V6 |
| NFR-CHAT-005（安全：判定链零变化 / 零明文 / 零注入） | V2② · V5 · V8 |
| NFR-CHAT-009（门禁串行 / 日志落盘 / 反证） | V1 |
| NFR-CHAT-010（设计契约条款化） | V1(design-contract 6/0) · V8 |
| NFR-CHAT-012（持久化边界） | V5 |
| EC-CHAT-001（超时 fail-closed / 不永久阻塞） | V2③ · V3(E7) |
| EC-CHAT-002（拾取 × 后台提问交错） | V3(E5) · V7 |
| EC-CHAT-003（会话切换不静默丢失） | V2①a · V3(switch) |
| EC-CHAT-009（固化卡可达性） | V1(ask-auth ⑧) · V2④ |
| EC-CHAT-013（叶内无法完成的显式登记） | V8（台账 / knownLimitations） |

---

## 3. 测试覆盖验证（覆盖度矩阵）

| FR/NFR | spec 描述 | 是否有测试 | 测试结果 | 覆盖率 |
|--------|----------|:--:|:--:|:--:|
| FR-CHAT-040~049 | 10 项（ask/auth 流内化 / 固化 / 超时·取消·取代留痕 / 授权回看 / 回合语义 / 零明文） | ✅ | 通过 | 已覆盖（`test/ask-auth-inflow.test.ts` 20 用例 + `test/ui/ask-auth-inflow.mjs` 61 断言 + l0/l1/stream/density 联动） |
| NFR-CHAT-001 | 留痕完备 | ✅ | 通过 | 已覆盖（终态矩阵 + 遍历探针） |
| NFR-CHAT-004 | 固化态无障碍 | ✅ | 通过 | 已覆盖（`aria-live` + 320/400/520 零溢出，ask-auth ⑧） |
| NFR-CHAT-005 | 安全零改动 / 零明文 / 零注入 | ✅ | 通过 | 已覆盖（零明文正反 + `test:zero-injection` 27/0 + 红线 0 diff） |
| NFR-CHAT-009 | 门禁纪律 | ✅ | 通过 | 已覆盖（22 门禁串行 + 反证日志） |
| NFR-CHAT-010 | 设计契约条款化 | ✅ | 通过 | 已覆盖（`test:design-contract` 6/0，shim 60/60） |
| NFR-CHAT-012 | 持久化边界 | ✅ | 通过 | 已覆盖（摘要白名单结构证明 + 反证） |

---

## 4. 性能与边界验证计划

| NFR / EC | spec 要求 | 测量方式 | 达标判据 |
|----------|----------|---------|---------|
| NFR-CHAT-004 / HO-1 / I-03 | 单卡可点 ≤6；首屏合计 ≤8 | Chromium `#stream` 可见可点计数（自写函数） | 收起/展开/两卡展开均达；自注入破坏必红 |
| 体积守卫（父收口锚） | sidepanel ≤ floor(baseline×1.05)=467,565；content 177,076 / pick-layer 33,900 无容差 | `stat` 实测 + 台账五要素 | 逐字节相等 / ≤ ceiling |
| EC-CHAT-001 | 60s 超时 fail-closed 且不永久阻塞 | 遍历 + 定向「不答+不取消+等超时」 | `pending` 回落；无未终态卡 |
| EC-CHAT-002 | 拾取与后台提问交错不丢过程 | R1 全链驱动 | 取代双留痕；无假超时 |

---

## 5. 漂移检测计划

| 漂移类型 | 检测方法 | 判据 |
|---------|---------|------|
| 孤立代码（有代码无需求） | 退役符号全仓 grep（`decision-card` / `turnStuck` / `STREAM_FIELD_WHITELIST` / `rounds[]` 差分） | 0 残留（注释除外） |
| 需求缺失（有需求无代码） | FR-CHAT-040~049 逐条映射实现落点 | 10/10 有落点 |
| 规格漂移（spec 被修改） | `git log -- spec.md / plan.md` + `git status` | 立项后零修改；工作树干净 |
| 红线漂移 | `git diff --numstat 0f8a1fb..HEAD` 对判定链/SW/内容脚本/manifest | 全为 0 |

---

## 6. 结论判定标准

| 条件 | 要求 | 本叶判据 |
|------|------|----------|
| 功能需求覆盖率 | 100%（每个 FR 有测试且通过） | 10/10 |
| 非功能需求覆盖率 | ≥ 80% | 6/6 = 100% |
| 构建通过 | 退出码 0 | typecheck + build 0 |
| 严重漂移 | 0 项 | 0 |
| 阻塞问题 | 0 项 | 0 |

**结论类型**：✅ 通过 / ⚠️ 有条件通过（非阻塞偏差）/ ❌ 不通过（未覆盖 FR / 构建失败 / 严重漂移）。

---

## 7. 修订记录

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| v1.0 | 初始创建（V1~V9 自主验证场景 + FR/NFR/EC→Vx 覆盖矩阵 + 五维度判据；与 validate-report.md 同轮产出） | 2026-09-19 | SDDU Validate Agent |
