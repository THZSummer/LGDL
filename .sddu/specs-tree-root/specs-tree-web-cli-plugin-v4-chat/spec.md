# Feature Specification：specs-tree-web-cli-plugin-v4-chat（web-cli-plugin v4「聊天流统一承载」）

> **文档定位**: SDDU 需求规范 — 定义功能需求、非功能需求和边界情况，作为 plan 阶段的输入
> **前置依赖**: 本目录 `discovery.md` v1.0（2026-09-18）——问题清单 **Q-CHAT-001~013**（核心 4 / 次要 5 / 潜在 4）/ 假设 **A-CHAT-001~009** / 风险 **R-CHAT-001~016** / 开放问题 **O-CHAT-001~009** / 现状基线 §7.1（全量 `file:line` 证据）/ 取代-保留清单草案 §7.1E / 叶子拆分草案 §7.6
> **设计基准**: `packages/web-cli-plugin/design/ui-redesign/option-f-chat-stream.html`（1,927 行，S1~S7 场景）+ `option-f-shim.mjs`（**60 断言 = 设计契约**，本轮实测 60 passed / 0 failed）；A~E 稿存档保留、不推翻
> **创建人**: SDDU Spec Agent
> **创建时间**: 2026-09-18
> **版本**: v1.0
> **更新人**: SDDU Spec Agent
> **更新时间**: 2026-09-18
> **更新说明**: 初始创建（v4「聊天流统一承载」需求规范：父 Feature = 轻量规范容器 + 4 个叶子子 Feature；含编排器对 O-CHAT 开放点的逐条裁决记录）

web-cli-plugin v4「聊天流统一承载」需求规范 —— 把侧栏从「L0/L1/L2 分区 + 浮层」重构为「**三区 + 聊天流统一承载**」：工具栏（常驻导航，≤5 可点）· 聊天流（唯一交互面，append-only 正序流）· 状态栏（常驻连接状态 + 风险 chips，永不折叠）；核心主张 = **一切交互皆消息**（7 主类卡 + 过程卡族，只追加、只固化）与 **留痕即事实**（过程性问答 / 授权记录 / 系统事件 / 引用生命周期全部在流内可回看）。编号一律 `FR-CHAT-*` / `NFR-CHAT-*` / `EC-CHAT-*` / `AC-CHAT-*` / `NG-CHAT-*`（与 v1/v2/v3 零冲突）。**父 Feature 定位 = 轻量规范容器**（不承接 tasks / build / review / validate），实施由 4 个叶子子 Feature 承载。

---

## 1. 元数据

| 字段 | 值 |
|------|-----|
| Feature ID | specs-tree-web-cli-plugin-v4-chat（web-cli-plugin v4「聊天流统一承载」，ROADMAP **F-30**） |
| 名称 | web-cli-plugin v4「聊天流统一承载」——把面板从「L0/L1/L2 分区 + 浮层」重构为「三区 + 聊天流统一承载」，一切过程交互留痕即事实 |
| 优先级 | P0（4 个叶子全部 P0） |
| 目标版本 | **v0.9.0** 同批叠加第三 Feature（先例：v0.7 = F-25+F-26 / v0.8 = F-14+F-27 / v0.9 = F-28+工程质量；同批合入与发布由作者决定） |
| 分支 | `feature/web-cli-plugin`（与 v1/v2/v3 同分支继续堆；**不合 main、不发布**） |
| 当前 HEAD | `187c205`（2026-09-18）；`main = 2ddc922`（不合、不发布） |
| 上游/底座 | v1 `specs-tree-web-cli-plugin`（`validated`）+ v2 `specs-tree-web-cli-plugin-v2-insight`（父 + 4 叶 `validated`）+ v3 `specs-tree-web-cli-plugin-v3-ui`（父 + 4 叶 `validated`，已收口）—— 全部**只读复用、零改写** |
| 目录深度 | depth=1（父 Feature，轻量规范容器）；子 Feature = 4 个**叶子**（depth=2，见 §11） |
| 相关干系人 | 作者（当前唯一真实用户 + 立项人，2026-09-17 真机反馈 + 2026-09-18 确认立项）；编排器（代作者决策 D1~D7 定论 + O-CHAT 裁决）；下游 @sddu-plan / @sddu-tasks / @sddu-build / @sddu-review / @sddu-validate |
| 关联问题 | Q-CHAT-001~004（核心，主干）/ Q-CHAT-005~009（次要）/ Q-CHAT-010~013（潜在） |
| 关联风险 | R-CHAT-001（门禁大面积落在被取代面）/ R-CHAT-002（journey 保护段语义失效）/ R-CHAT-003（DOM id 契约迁移）/ R-CHAT-004（密度口径重定义）/ R-CHAT-005（风险位形态）/ R-CHAT-006（append-only vs 可变 state）/ R-CHAT-007（busy/回合语义）/ R-CHAT-008（零明文冲突）/ R-CHAT-009（`KIND_SET` 冻结面）/ R-CHAT-010（体积绝对上限未定）/ R-CHAT-011（工具栏准入缺失）/ R-CHAT-012（拾取入口消失）/ R-CHAT-013（分类学缺口）/ R-CHAT-014（固化可交互性/无障碍）/ R-CHAT-015（测试守恒压力）/ R-CHAT-016（门禁资源约束） |

---

## 2. 上下文

### 2.1 立项来源（**作者原话逐字留存，不得转述走样**）

| 日期 | 来源 | 原话（逐字引用） |
|------|------|----------------|
| 2026-09-17 | 作者真机反馈（立项主诉） | 「**所有过程性的问答，建议放到聊天框里面，同时也方便看到授权记录，不要放到上面，丢失过程信息，每一步交互，尽可能都在聊天框留痕，作为事实依据，包括推荐的下一步操作，都放到聊天框里面**」 |
| 2026-09-17 | 作者布局主张 | 「**整体布局简化，工具栏、聊天框、状态栏**」 |
| 2026-09-17 | 作者内容主张 | 「**聊天框包含：AI答复聊天、下一步推荐、问答、权限申请**」 |
| 2026-09-17 | 作者设计指令 | 「先在 design/ui-redesign 输出一份方案F吧，输出完对着它进行重构」 |
| 2026-09-18 | 作者确认 | 「**开启重构的SDDU流程**」 |

作者是插件当前**唯一真实用户**（A-CHAT-008；样本量为 1 为已知局限，如实登记）。真机痛点现场（事实，不美化）：翻译指令 → `ask-user` 编号提问**浮在上层**（`#l0-decision`，聊天流之上）→ 回答「页面原地翻译」→ **引用 1 失效**（选择器断链、目标文字仍可见——R3 已修判定语义）→ 「重新拾取 / 改用描述」→ AI 又追问一轮 → **整段过程散落无留痕**，即核心问题 **Q-CHAT-001~004**。

### 2.2 编排器裁决承接（**定论，直接作为需求约束，不重新讨论**）

| # | 决策 | 在本规范中的承载体 |
|---|------|------------------|
| **D1** | Feature 命名 = `specs-tree-web-cli-plugin-v4-chat`，继承 v1/v2/v3 命名惯例 | §1 / §2.3 |
| **D2** | ROADMAP 编号 = **F-30**（`F-29` 为 A2A 候选保留；本轮实测 `F-30` 零占用） | §1 / §11 |
| **D3** | 版本落点 = **v0.9.0 同批叠加第三 Feature**（由 discovery 复核后建议采纳） | §1 |
| **D4** | 设计基准 = F 稿 + shim 60 断言 = 设计契约；A~E 稿存档保留、不推翻 | §2.5 / FR-CHAT-001 / AC-CHAT-025 |
| **D5** | **设计五法则为红线**（法一~法五，见 §2.5），任何实施不得违反；本 Feature **新增法则六**（见 §2.2 裁决 R 系列 / §5.2） | §5.2 / §8.2 / AC-CHAT-004~008 |
| **D6** | **不动面**零改动：`dist/content.js` 177,076 B、`dist/pick-layer.js` 33,900 B 冻结；判定链 pin（`policy.ts` `bfcb2ede…` / `auto-authorize.ts` `1096d065…`）；manifest 零新增权限 + 无 `contextMenus`；**风险永不折叠**原则；R1/R2/R3 修复语义 | FR-CHAT-003 / FR-CHAT-004 / FR-CHAT-005 / FR-CHAT-006 / AC-CHAT-020 / AC-CHAT-021 |
| **D7** | **随带硬义务 V3-VOL-3**：收口重定 sidepanel 体积基线 + 设绝对值上限（`PENDING_ABSOLUTE_CAP` 带值闭合）；取代走取代台账；密度门禁按新布局重新定标 | §5.9 / §5.10 / §10 / AC-CHAT-017 / AC-CHAT-018 |

**编排器对 O-CHAT 开放点的裁决**（2026-09-18，作者已授权全流程自行调度；逐条理由见 §12 §裁决记录）：

| 裁决 | 内容 |
|------|------|
| **法则六（新增）** | 「**过程入流，管理入视图**」——需要 AI 会话上下文的过程性交互（问答 / 确认 / 授权申请 / 撤销 / 引用）入流内卡；与单次会话无关的面板管理操作（会话切换 / 隐私开关 / 知情同意管理 / 自动授权开关 / 站点列表管理）归工具栏视图（设置 / 审计）。解决 **O-CHAT-005** |
| **卡分类学** | **7 主类 + 过程卡族**（O-CHAT-008）：F 稿 7 类为「主类」；现状 `#log` 内既有 tool 卡（ok/ms/预览/折叠记忆）、命令行、思考指示、错误条目、工具通知 5 种形态归并为「**过程卡族**」（AI 消息族的子形态），**全部 append-only 留痕、同样适用固化契约**——不许丢弃任何既有过程形态 |
| **密度新口径** | 测量根从 `document.body` 改为**三区外壳**（工具栏 + 状态栏 + 流容器的骨架层）；流内容不计入默认密度（法五），但新增两条防滥用约束：① **单张卡内部可点元素 ≤6**（超出 = 拆卡或入视图）② **首屏（空流欢迎态）卡片数 ≤2**；豁免边界仍只认 `hidden`；防滥用约束必须配可 FAIL 反证；阈值哲学 `7/15 · 9/20 · 17/35` 保留，全部登记格重算（解决 R-CHAT-004） |
| **journey 保护段处置** | R-CHAT-002：v2 量化红线「流区高度占比 ≥65% / 高度不回退」**语义保留**；「composer 贴底 ∈[0,+8px]」被法则四（无常驻输入框）**显式取代**——走取代台账（`docs/v3-supersession-ledger.json` 登记，**保护段哈希变更必须台账留痕，禁静默改写**） |
| **叶子拆分** | 采纳 discovery 草案：v4-1 三区骨架/工具栏/状态栏/密度重定标 · v4-2 append-only 事件模型 + 卡渲染 · v4-3 ask/授权流内化 + 留痕固化 · v4-4 引用卡/系统事件行/推荐卡 + 拾取入口迁移；**V3-VOL-3（重定基线 + 绝对上限带值闭合）为父级收口锚**，由最后完成的叶执行；各叶轮内按五要素做中间重登记 |
| **绝对上限数值来源** | **O-CHAT-004**：spec 记录**推导规则**而非预填值（PENDING 机制禁止预填）——收口时按「重构落地实测值向上取整到 **50 KB 档** + **10% 余量**」推导，交作者一句确认后闭合；**不设先验魔法数** |
| **O-CHAT-009 竞品调研** | 设计已由作者定向（F 稿定稿），竞品调研**非阻塞**，登记为「**未执行，不阻塞**」——**不编造任何竞品结论** |

### 2.3 编号命名空间声明（**强制**）

| 命名空间 | 本 Feature 使用 | 历史占用（**零冲突，禁止复用**） |
|---|---|---|
| 功能需求 | `FR-CHAT-###` | v1 `FR-001~FR-055`；v2 `FR-V2-001~079`；v3 `FR-V3-001~087` |
| 非功能需求 | `NFR-CHAT-###` | v1 `NFR-001~010`；v2 `NFR-V2-001~010`；v3 `NFR-V3-001~018` |
| 边界情况 | `EC-CHAT-###` | v1 `EC-001~026`；v2 `EC-V2-001~016`；v3 `EC-V3-001~017` |
| 验收标准 | `AC-CHAT-###` | v1 `AC-001~012`；v2 `AC-V2-001~027`；v3 `AC-V3-001~027` |
| 非目标 | `NG-CHAT-###` | v2 `NG-V2-001~009`；v3 `NG-V3-001~010` |
| 其他 | `G-CHAT-###` / `US-CHAT-###` / `DC-CHAT-###`（裁决记录）/ `Q-CHAT-###` `A-CHAT-###` `R-CHAT-###` `O-CHAT-###`（沿用 discovery） | — |

### 2.4 目标用户

| 角色 | 场景 | 本 Feature 的改善目标 |
|------|------|---------------------|
| **日常站点使用者（首位真实用户 = 作者本人）** | 在已授权站点让插件「把这页原地翻译」；过程被问 3 个编号问题、拾取引用、批准一次写操作 | 「每一步交互都在聊天框留痕，作为事实依据」——过程在流内可回看（Q-CHAT-001/002） |
| **需要复核自己授权的使用者（同一人，风险场景）** | 批准 `dom.click` 单次授权后想确认「批准了什么、批准了几次」 | 授权记录在流内可当场回看；完整台账仍走工具栏「审计」视图（Q-CHAT-002 / 法则二） |
| **窄侧栏使用者（320–560px，窄屏优先）** | 侧栏拖窄到 320px 仍要完成同一批任务 | 三区新布局仍零水平溢出；流内卡在 320px 可读（NFR-CHAT-002 / EC-CHAT-004） |
| **新用户 / 生态用户（后续 web-cli 站点用户）** | 首次安装：onboarding + discovery-notice 后「下一步做什么」为空槽 | 「下一步推荐」等推荐卡（chips 即指令）把能力发现变成被引导（Q-CHAT-003） |
| **维护者 / 开发者（AI Agent + 作者）** | 每次 UI 改动都要过 12 道门禁（含 9 个 Chromium 门禁） | 取代台账 + 计数不减 + 密度新口径机器门禁（含反证）；每叶可独立全门禁绿（Q-CHAT-010 / R-CHAT-015/016） |

### 2.5 设计基准与设计契约（**60 断言 = 契约，逐条条款化**）

**三区骨架**（F 稿 `:33-46` 逐字口径）：

```
┌ 工具栏（顶部，一行，常驻）─ 站点摘要（只读 status）+ 常驻导航（树/命令/审计/设置）
│                            可点 ≤5；禁止：任何一次性交互（问答/授权/推荐）
├ 聊天流（主体，唯一交互面）─ 追加式正序流，自动滚底、可上滚回看（=留痕）
│                            一切交互皆 7 类消息卡之一
└ 状态栏（底部，常驻，永不折叠）─ ① 连接状态一行 ② 活跃风险 chips（有则显示）
                                  点 chip → 展开详情 + 滚动定位到流内相关卡
```

**7 类消息卡契约**（F 稿 `:48-60`）：`ai` / `user` / `nextstep`（chips 即指令）/ `askuser`（应答或取消后**固化不可逆**）/ `auth`（批准或拒绝后**固化不可逆**）/ `system`（单行 + 时间戳，只追加）/ `ref`（有效/失效，失效卡保留，新引用序号递增）。固化态统一契约 = `data-*` 状态属性 + `[hidden]` 切换表单/固化区 + `.ts` 时间戳 + `.card-fixed`；**不存在「撤销回答 / 撤销批准」控件 —— 撤销本身是一条新的系统事件行**。

**五条设计法则（红线，F 稿 `:62-74` 逐字口径）**：

| 法则 | 内容 |
|------|------|
| 法一 | **一切交互皆消息**：问答 / 授权 / 推荐 / 系统事件一律入流；一次性交互禁止出现在工具栏或浮层（可静态核验） |
| 法二 | **留痕即事实**：卡片只固化不撤销（回答 / 批准 / 失效都是既成事实）；流 = 会话审计线索；完整台账仍走工具栏「审计」视图 |
| 法三 | **三区各司其职**：工具栏只放常驻导航与站点摘要；状态栏只放常驻状态与风险；一切「过程」进流 |
| 法四 | **输入即 next**：自由文本输入是流内 next 的一个选项；**流外零输入面**（★ IAN-2 原地修订：旧「输入按需出现：无常驻输入框…」→ 新条文；`ask-user` text 型输入框只在流内卡内出现，流外无任何输入面） |
| 法五 | **默认密度继承**：默认屏可点元素 = 工具栏 + 状态栏合计 **≤7**；**聊天流内容不计入**（它是记录，不是控件预算） |
| **法六（新增）** | **过程入流，管理入视图**：需 AI 会话上下文的过程性交互入流内卡；与单次会话无关的面板管理操作归工具栏视图（设置/审计） |

**设计契约机器化（`option-f-shim.mjs`，本轮实测 60 passed / 0 failed）**：断言分 9 组共 **60** 条 —— `A1~A6`（三区结构与语义，6）· `B1~B4`（卡样例与流挂载，4）· `C1~C11`（ask-user 结构 + 操作前→后固化，11）· `D1~D7`（授权卡结构 + 两态固化，7）· `E1~E5`（系统事件时间戳 + 引用失效/重拾，5）· `F1~F7`（密度预算 + 法则一/四，7）· `G1~G7`（三宽度 × 三主题，7）· `H1~H12`（场景 S2/S5/S6/S7 + L2 视图 + 风险 chips 永不折叠，12）· `I1`（收尾复位，1）。**逐条 → 契约条款 → 承载 FR/AC 的映射见附录 A**（§15）。

> **纪律**：shim 是**设计稿自带的 Node DOM 垫片**（非项目 Chromium 门禁，零副作用、零构建、未改 dist）。spec 把 60 断言**条款化**为上表与附录 A；**真实产物**上的等价验收由各叶以项目门禁承载（AC-CHAT-025 只要求设计契约机器化 60/60 + 真实产物侧按条款复算）。

### 2.6 现状事实核对（**spec 阶段实测，只读**）

| 项 | 事实 | 来源 |
|---|---|---|
| `dist/content.js` | **177,076 B = `CONTENT_MAX_BYTES`**（**无容差，+1 B 即 FAIL**）；`CONTENT_SOURCE_SHA256` 冻结 | `test/size-baseline.ts:13` + discovery §7.1D |
| `dist/pick-layer.js` | **33,900 B**（自有「不增长」上限） | `test/size-baseline.ts:458` |
| `dist/sidepanel.js` | **375,102 B**（= `SIDEPANEL_BASELINE_BYTES`）；ceiling = `floor(375,102 × 1.05)` = **393,857**（容差 5%） | `test/size-baseline.ts:286-289,318-320` |
| v3 累计增长 | **+40.75%**（266,500 → 375,102）；最差相邻对 +22.96% | `SIDEPANEL_BASELINE_META.consecutiveGrowthAlert` |
| **V3-VOL-3 裁决** | Feature 级 40% 累计停工线**显式撤销**（`enforced:false` + `revokedBy:'V3-VOL-3'` + 可 FAIL 反证 `wouldFail`）；**保留**单轮五要素披露纪律 | `size-baseline.ts:401-414,1236-1285`；`test/size-ruling-vol3.test.ts`（6 用例） |
| **`PENDING_ABSOLUTE_CAP`** | `resolved:false`；三值全 `null`；纯判定 `evaluatePendingAbsoluteCap()`：标记缺失 ⇒ FAIL / `resolved:false` 禁止预填 / `resolved:true` 必须三值齐备且 `absoluteCeilingBytes ≥ newBaselineBytes` 且 `resolvedOn` ∈ `YYYY-MM-DD` | `size-baseline.ts:1359-1378,1387-1440` |
| 已撤销旧机制 | `SIDEPANEL_CEILING_CAP` 判定作用已被 **V3-VOL-1 ②** 撤销（`record-only`），理由 = 未经要求的自缚装置 | `size-baseline.ts:146-176` |
| 保护段 1 | `test/ui/journey.mjs` 字节 `42766..54004`，sha256 `6b45c3fa…`，185 行（`#15a~#15q`，读 `#log`/`#composer` 几何） | `docs/v3-supersession-ledger.json#protectedRanges` |
| 保护段 2 | `test/ui/binding.mjs` 字节 `107780..115930`，sha256 `be9ad0e9…`，184 行（`#22a~#22l`） | 同上 |
| 门禁末轮计数 | journey **167** / insight **116**（下界 108）/ binding **192** / hardening **24** / l0 **164**（叶下界 73）/ l1 **103**（叶下界 64）/ l2 **71**（叶下界 68）/ zero-injection **27** / page-input **102** / density **127**（叶下界 60）/ gate-integrity **12**（判定器 selftest 15）/ sidepanel-view **38** / nodeTestRuntime **832** | v3 `closeout.md:13,50-55` + 台账 `counts` |
| 取代台账 | `docs/v3-supersession-ledger.json`：126 entries；`protectedRanges`（2 段）+ `leafBases`（叶段判据）+ `v3ReverseProofExpectations`（期望文本下界） | 本轮读取 |
| 消息契约冻结面 | `KIND_SET`（`background/messaging.ts:103+`）打包进 `dist/content.js`；先例：新增 6 个 kind 实测 **+307 B = 红线突破** → 新面一律走**独立校验器**（`pick-protocol.ts:9-12`） | discovery §7.1C / Q-CHAT-012 |
| 现状侧栏 DOM | `src/ui/sidepanel/index.html` 68,408 B / **109 个 `id`** | discovery §7.1 |
| 设计稿 | F 稿 116,130 B / 1,927 行；shim 33,368 B；全部已入版本库（A~E 稿存档保留） | 本轮 `stat` / `git ls-files` |
| manifest | 静态 `activeTab/scripting/storage/sidePanel/tabs`；可选 `bookmarks/downloads/notifications/clipboardRead/clipboardWrite`；**无 `contextMenus`**；无静态 `content_scripts` | `manifest.json`（未改） |

### 2.7 与 v1/v2/v3 的关系（**边界**）

| 上游 | 保留（v4 继续成立） | 取代/重定标（v4 改变承载或语义） |
|------|-------------------|------------------------------|
| **v1**（F-14） | 三区 flex 全高布局概念；消息渲染（Markdown/工具卡折叠/命令样式）；`scroll-policy`（48 px 跟随）；会话/审计/设置/权限门禁语义；`FR-050`（失败工具卡可见）；会话切换能力 | `AC-001~012` 中与**布局/入口位置**相关的量化断言（三区 = `#panel-top`/`#panel-main`/`#panel-bottom`、composer 末元素、`#log` 唯一滚动区）→ 由三区新定义取代；`#15a~#15q` 几何断言重定标 |
| **v2**（F-27） | 四维度数据模型与投影（`insight/*`）；真层级树 + 逐层 allow/ask/deny + 硬底线 clamp（**判定链 pin 不动**）；撤销/取消授权 ops；命令档案对账；零明文与零新权限纪律 | **`AC-V2-002` / `FR-V2-023` / `NFR-V2-004`**（`#log flex-grow=1`、`clientHeight ≥589px`、占比 ≥65.0%、composer 底边−视口底 ∈[0,+8px]、入口与 composer 不相交）→ 与 F 三区/流内 composer 语义**直接冲突**，必须显式取代（§10）；`#tree-fab` 归属 → 迁为工具栏入口 |
| **v3**（F-28） | 视觉语言与设计令牌（双主题对称）；`l2/counts.ts` 计数单源；`l2/view-host.ts` 视图替换机制；`l1/ref-store.ts` 五维判定 + 序号单调不复用 + 退役留痕；`l1/ref-validity.ts` fail-closed；零明文回执；`page-as-input`（v3-4 页面侧层 + 零注入）；`disclosure.ts` 的 `hidden` 唯一折叠通道纪律 | **整个 L0 骨架**（`#l0-status-band`/`#l0-decision`/`#l0-kicker`/`#l0-pick`/`#l0-more`/`#l0-ref-toggle`/决策卡唯一性）→ 被三区取代；**整个 L1 层**（8 类 `[data-l1-panel]` + 三触发器 + `L1_TRIGGERS` + ≤1 次交互契约）→ 承载位置改为流内卡；**L2 入口机制**（`#l0-statusbar` + `#l2-entries` + 四入口 aria 成对）→ 迁到工具栏；**风险位形态**（`#risk-rail` body 直挂 + 五类行 + 密度风险类归属）→ 迁为状态栏 chips（永不折叠语义保留）；**密度口径与全部登记格** → 重定标 |
| **v1/v2/v3 共同** | 不动面：判定链 pin、manifest 零新增权限、`content.js` 177,076、`pick-layer.js` 33,900、零明文纪律、fail-closed 结论枚举、R1/R2/R3 修复语义、取代台账 + 门禁守恒纪律 | — |

---

## 3. 目标与非目标

### 3.1 目标 (Goals)

| # | 目标描述 |
|---|---------|
| G-CHAT-001 | **过程留痕**：把过程性交互（问答 / 确认 / 授权申请 / 撤销 / 引用生命周期 / 系统事件）从「流之上的槽位 + 底部瞬时条」搬进聊天流，且**只追加不留痕丢失**——用户可在同一界面回看「我是怎么走到这一步的」 |
| G-CHAT-002 | **结构与心智稳定**：把布局收敛为「工具栏 / 聊天流 / 状态栏」三区，工具栏只放常驻导航（≤5 可点）、状态栏只放常驻状态与风险、一切过程进流 |
| G-CHAT-003 | **授权可回看**：破坏性确认与单次操作授权的**批准/拒绝**在流内固化可回看；完整台账仍走审计视图 |
| G-CHAT-004 | **下一步推荐**：新增「推荐卡（chips 即指令）」承载能力发现，把「下一步做什么」从散落的静态字符串变成一条可点的流内消息 |
| G-CHAT-005 | **能力集等价**：既有全部过程能力（工具卡 / 命令行 / 思考 / 错误 / 通知 / 引用 / 回执 / 树 / 命令目录 / 审计 / 设置）**零丢失**地获得新承载面（过程卡族 + 视图） |
| G-CHAT-006 | **可机器验证**：密度新口径 + 防滥用 + 取代台账 + 体积绝对上限，全部**可复算、可 FAIL、配反证**，测试计数只增不减 |
| G-CHAT-007 | **不动面守住**：`content.js` 177,076 B / `pick-layer.js` 33,900 B / 判定链 pin / manifest 零新增权限 / 风险永不折叠 / R1/R2/R3 语义零漂移 |

### 3.2 非目标 (Non-Goals)

| # | 明确不做 |
|---|---------|
| NG-CHAT-001 | 不推翻 A~E 设计稿与 v3 视觉语言（设计令牌 / 卡片形态 / 双主题 / 字体与滚动条口径）；A~E 稿**存档保留** |
| NG-CHAT-002 | 不动面（零改动）：`dist/content.js` 177,076 B 与 `dist/pick-layer.js` 33,900 B 冻结；判定链 pin（`policy.ts`/`auto-authorize.ts`）；manifest 零新增权限、无 `contextMenus`；风险永不折叠原则；R1/R2/R3 修复语义 |
| NG-CHAT-003 | 不改判定链语义、不放宽 fail-closed 结论枚举、不降级任何安全/硬底线断言 |
| NG-CHAT-004 | 不以放宽密度/体积上限换功能；不把「密度低」实现为「删功能」；不降级既有断言 |
| NG-CHAT-005 | 不新增依赖、不做后端/服务端；不改 `packages/web-cli-base/**`；不改 `main`；不改 v1/v2 SDDU 目录（除只读）；不改 `design/**`（除只读引用） |
| NG-CHAT-006 | **不丢弃任何既有过程形态**（工具卡 / 命令行 / 思考指示 / 错误条目 / 工具通知）——全部归并为过程卡族并保留其信息 |
| NG-CHAT-007 | 不设未经 spec/作者要求的自缚装置（V3-VOL-1 ② 教训：`SIDEPANEL_CEILING_CAP` 曾被撤销） |
| NG-CHAT-008 | 不静默改写受哈希保护的门禁区段；保护段任何变更必须台账留痕 |
| NG-CHAT-009 | 不编造任何未实测数字 / 不编造竞品结论 / 不在 `PENDING_ABSOLUTE_CAP` 未闭合时预填值 |
| NG-CHAT-010 | 不新增任何 manifest 权限；不引入 `contextMenus`（主题切换等新 UI 只能用已有 `storage` 能力） |
| NG-CHAT-011 | 本 spec 阶段**零实现承诺**：不写 plan/ADR/技术方案、不排任务、不改任何生产代码/测试/dist/ROADMAP |

---

## 4. 用户故事

| # | 作为… | 我想要… | 以便… |
|---|-------|---------|-------|
| US-CHAT-001 | 日常站点使用者 | 在聊天流里看到 AI 问我的每一个问题和我的每一次回答（已答后固化原位保留） | 我能回看「我是怎么走到这一步的」，把它当作事实依据 |
| US-CHAT-002 | 需要复核授权的使用者 | 在聊天流里看到我批准/拒绝了什么操作、什么时候批准的 | 高风险操作有可当场核对的记录 |
| US-CHAT-003 | 日常站点使用者 | 拾取引用后，在流里看到引用卡；引用失效后失效卡保留、重拾生成新序号 | 引用生命周期可见，不会「解析即消失」 |
| US-CHAT-004 | 新用户 | 在流里看到「下一步可以做什么」的可点 chips | 不用自己猜下一步，能力发现被引导 |
| US-CHAT-005 | 窄侧栏使用者 | 在 320px 宽下完成同一批任务且不出现水平滚动 | 窄屏优先的可用性不因重构回退 |
| US-CHAT-006 | 日常站点使用者 | 系统事件（导航失效 / 探测退避 / 页面侧不可用）以带时间戳的单行出现在流里 | 异常过程有顺序、可回看，不被下一次刷新覆盖 |
| US-CHAT-007 | 维护者 | 每个叶子的取代被台账登记、计数只增不减、密度/体积门禁可复算可 FAIL | 重构不以测试强度或安全为代价 |

---

## 5. 功能需求 (FR)

> 编号前缀 `FR-CHAT-`；每条可测试。优先级：全部 P0（本 Feature 为 P0 重构），标注 P1 者为叶内可收紧项。

### 5.1 GOV — 立案、编号与纪律（横切）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-001** | 本 Feature 以目录 `.sddu/specs-tree-root/specs-tree-web-cli-plugin-v4-chat/`、ROADMAP 编号 **F-30**、目标版本 v0.9.0 同批叠加登记；编号体系采用 §2.3 命名空间 | 目录/编号/F-30 零冲突核验；编号前缀 grep 零串用 | P0 |
| **FR-CHAT-002** | 范围 = 「三区 + 聊天流统一承载」重构；上游 v1/v2/v3 已验收实现**只读复用**，上游 SDDU 目录零 diff | `git diff` 上游 SDDU 目录 = 空 | P0 |
| **FR-CHAT-003** | **不动面零改动**：`dist/content.js` 177,076 B（无容差）、`dist/pick-layer.js` 33,900 B 逐字节不变；`manifest.json` 零 diff（零新增权限、无 `contextMenus`、无静态 `content_scripts`）；判定链 pin（`policy.ts`/`auto-authorize.ts` sha256）不变 | 体积门禁 + sha256 核对 + manifest 零 diff | P0 |
| **FR-CHAT-004** | **风险永不折叠**为语义约束：五类风险（未授权 / 探测中 / 硬底线被拦 / 破坏性待确认 / 引用失效）在任何展开/收起/视图切换下**始终可发现**，且不得被实现为「藏进展开层」；新形态（状态栏 chips）须定义新机器判据（祖先闭包 + 状态栏本体无 `hidden` + 视图打开后 chips 仍可见） | 风险门禁（AC-CHAT-007 / AC-CHAT-022） | P0 |
| **FR-CHAT-005** | **R1/R2/R3 修复语义保留**：R1 declaration 状态一致性（无有效声明时拾取引用「出生即死」的单一事实源）、R2 探测指数退避 + 稳态去闪烁、R3 引用重锚救援（选择器断链时文本候选定位 + 唯一匹配一键重锚；旧引用零改动） | 回归门禁（binding 相关断言只增不减 + R3 旧引用零改动断言） | P0 |
| **FR-CHAT-006** | **fail-closed 判定链语义不变 + 零明文纪律不变**：不确定即 deny/失效；回执白名单 8 字段 + `assertNoPlaintext` 抛错；审计字段白名单 + URL 去参；key / 剪贴板 / 通知 / 书签正文永不入明文（含流内留痕） | 安全反向断言 + 零明文门禁（AC-CHAT-021） | P0 |
| **FR-CHAT-007** | **纪律**：测试计数只增不减、断言只强不弱；门禁严格串行（一次一个 Chromium）、日志完整落盘（禁 tail 截断）；反证必须实跑（FAIL → 还原 → PASS）；禁 `git add -A`；不改 `main` | 门禁日志 + 反证日志 + `git status` 核对（AC-CHAT-019 / AC-CHAT-023） | P0 |

### 5.2 ZONE — 三区布局与工具栏准入（叶子 v4-1）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-010** | **三区骨架**：工具栏（`header[role=toolbar]`，常驻一行）→ 聊天流（`main` 内含 `ol#stream[role=log]`，唯一交互面）→ 状态栏（`footer[role=contentinfo]`，常驻）三区齐备且**顺序正确** | 三区结构/顺序/语义断言（对应 shim A1~A6） | P0 |
| **FR-CHAT-011** | **工具栏准入 ≤5 可点**：工具栏 = 站点摘要（**只读** `role=status`，不计入可点预算）+ 4 个视图入口（树/命令/审计/设置，带计数徽标）+ 1 个主题切换 = 5；**准入规则**：常驻入口必须「与单次会话无关」且属于四视图/主题/站点摘要；满额时新增入口必须**显式置换**（登记 old→new + 理由），禁止静默第 6 个 | 工具栏可点计数 ≤5 + 准入规则登记（shim F1/F4/F5；AC-CHAT-008） | P0 |
| **FR-CHAT-012** | **法一可静态核验**：工具栏与状态栏内**不存在** ask-user / 授权 / 推荐类一次性交互卡（或其控件） | 静态核验断言（shim F6；AC-CHAT-004） | P0 |
| **FR-CHAT-013** | **状态栏常驻**：① 连接状态一行；② 活跃风险 chips（有则显示、无则收缩为一行、0 个可点 chip）；状态栏本体**无 `hidden`**、chips 容器可见 | shim F3/H4/H5/H8/H12；风险门禁 | P0 |
| **FR-CHAT-014** | **法四（输入即 next）**：自由文本输入是流内 next 的一个选项；**流外零输入面**（默认屏无输入面，`ask-user`/free-input 卡内输入是唯一载体） | shim F7 / C1 / C4（AC-CHAT-007） | P0 |
| **FR-CHAT-015** | **L2 视图替换机制复用**：四视图打开时视图层可见、流隐藏，标题正确；返回后视图层收起、流恢复（同一内容区替换，非浮层） | shim H9/H10（AC-CHAT-002 / FR-CHAT-011） | P0 |
| **FR-CHAT-016** | **法则六落位（管理入视图）**：站点级授权 / 撤销授权 / 会话切换与分组 / 隐私开关 / 知情同意 / 自动授权开关等**与单次会话无关的管理操作**，统一落在工具栏可打开的**设置/审计视图**内；工具栏站点摘要保持只读；未授权首装路径必须存在**可发现**的授权入口（视图内） | 管理操作归属核验 + 首装路径可发现性断言（AC-CHAT-006；解决 Q-CHAT-013 / O-CHAT-005） | P0 |
| **FR-CHAT-017** | **风险位形态迁移合规**：风险从顶部 `#risk-rail` 迁为状态栏 chips，**位置/形态/交互可变**但「永不折叠」语义不变；chip 点击展开详情 + 滚动定位到流内相关卡；详情展开**不占默认密度**；展开详情含「本阶段不发命令、不改授权」披露语 | shim H4/H5/H6/H7/H8/H12；风险门禁（AC-CHAT-007） | P0 |

### 5.3 STREAM — append-only 事件流模型（叶子 v4-2）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-020** | **流事件模型不可变、单调、可回放**：流条目只追加；id 单调递增（统一 `nextId` 口径）；**不存在**「解析即置 `null`」导致条目从界面消失的路径；同一事件序列可机器回放得到同一可见流 | 模型单测（append-only / id 单调 / 回放等价）+ 无「置 null」路径断言（AC-CHAT-010） | P0 |
| **FR-CHAT-021** | **流条目类型集合**：7 主类（`ai`/`user`/`nextstep`/`askuser`/`auth`/`system`/`ref`）+ **过程卡族**（`tool`/`command`/`thinking`/`error`/`notice`，AI 消息族子形态）全部为合法流条目类型；类型集合单一登记，扩展须显式登记 | 类型集合断言 + 每类样例可见（shim B1/B2/H11；AC-CHAT-002） | P0 |
| **FR-CHAT-022** | **固化契约统一**：操作前 → 操作后通过 `data-*` 状态属性 + `[hidden]` 切换表单/固化区 + `.ts` 时间戳 + `.card-fixed` 表达；固化文案与状态属性一一对应 | shim C5~C10 / D2~D6 / E5（AC-CHAT-003） | P0 |
| **FR-CHAT-023** | **只固化不撤销**：回答 / 批准 / 拒绝 / 取消 / 失效均固化不可逆；**不存在「撤销回答 / 撤销批准」控件**；撤销本身是一条**新的系统事件行** | 控件不存在断言 + 撤销生成事件行断言（AC-CHAT-005） | P0 |
| **FR-CHAT-024** | **会话切换不得丢过程事实**：`history` 动作（会话切换）**不得**丢工具卡元数据（`tool`/`ok`/`ms`）与过程条目；会话切换后的流保留过程可回看（口径：至少保留会话内事件；持久化范围见 §13） | 会话切换回归断言 + 元数据不丢断言（AC-CHAT-010；解决 O-CHAT-002③） | P0 |
| **FR-CHAT-025** | **留痕完备性**：问答 / 授权申请与决策 / 引用生命周期 / 系统事件 / 推荐 五类过程在流内均**有对应条目且可回看**；完整台账仍走工具栏「审计」视图（流为会话审计线索，审计为全量台账） | 留痕完备性矩阵断言（AC-CHAT-010 / AC-CHAT-016） | P0 |
| **FR-CHAT-026** | **消息契约扩展不得进 `KIND_SET`**：新增 SW↔面板 kind 一律**不进** `KIND_SET`（该集合进 `dist/content.js`，+1 B 即 FAIL），沿用**独立校验器**模式（先例 `insight-protocol.ts` / `content/pick-protocol.ts`） | `KIND_SET` 零 diff + 独立校验器存在断言（AC-CHAT-020） | P0 |

### 5.4 CARD — 7 主类 + 过程卡族契约（叶子 v4-2）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-030** | 7 主类卡样例全部存在且**挂在流 `#stream` 的 `li` 上**（不是流之外的槽位） | shim B1/B2/H11 | P0 |
| **FR-CHAT-031** | `ai` 卡支持富文本（编号列表 / 代码 / 表格摘要）；`user` 卡为对侧气泡 | shim B3 + 现有渲染复用断言 | P0 |
| **FR-CHAT-032** | `system` 卡为**单行 + `HH:MM:SS` 时间戳**，只追加；同屏 ≥3 条时可区分 | shim E1 | P0 |
| **FR-CHAT-033** | `ref` 卡区分有效/失效；**失效卡保留**（不删除）；重拾后**新引用序号递增**（旧卡零改动） | shim E5/E2/E3 | P0 |
| **FR-CHAT-034** | `nextstep` 卡含可点 chips（chips 即指令） | shim B4（AC-CHAT-013） | P0 |
| **FR-CHAT-035** | **过程卡族归并**：`#log` 内既有 5 种形态（工具卡 / 命令行 / 思考指示 / 错误条目 / 工具通知）全部归为过程卡族并保留信息 —— 工具卡保留「工具名 + ✓/✖ + 耗时(`ms`) + 预览 + 折叠记忆（480 字符 / 10 行）」；**全部 append-only 留痕、适用固化契约** | 5 形态逐项存在 + 工具卡信息字段断言（AC-CHAT-002 / AC-CHAT-003） | P0 |
| **FR-CHAT-036** | **分类学登记**：7 主类 + 过程卡族的类型清单、`CARD_TYPES` 的扩展方式、shim 契约变更的登记方式（若扩展，必须登记设计契约变更而非静默改断言） | 类型登记清单 + 契约变更登记（AC-CHAT-025） | P0 |
| **FR-CHAT-037** | 每张卡具备可访问语义：卡的 `role`（或等价结构）+ `.ts` 时间戳可被读屏访问；流为 `role=log` 的 `ol`，正序追加 | 无障碍断言（NFR-CHAT-004） | P0 |

### 5.5 FLOW — ask-user 与授权卡流内化（叶子 v4-3）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-040** | `ask-user`（choice / text 两型）从**流之上**的独占决策槽迁入**流内**卡；不再有「同时只允许一张、旧卡不滞留」的独占语义 | shim C1/C2/C11 + 独占槽移除断言（AC-CHAT-003） | P0 |
| **FR-CHAT-041** | `ask` 卡操作前/后固化：未答时表单可见、固化区 `hidden`；回答后 `data-answered=true`、表单收起、固化区显示「已答：+ 回答内容」+ 时间戳 | shim C5~C7 | P0 |
| **FR-CHAT-042** | 已固化 `ask` 卡**不可二次回答**；「取消」也留痕（固化文案「已取消（不代填默认值）」） | shim C9/C10 | P0 |
| **FR-CHAT-043** | `ask` 卡 60 s 超时 → 结算为 `canceled` 并**留痕为事件/固化态**（保持 fail-closed，不代填默认值） | 超时留痕断言（AC-CHAT-015） | P0 |
| **FR-CHAT-044** | **`supersededAsk` 升级为「取消 + 留痕」**：被取代的后台提问除 settle 为 `canceled` 外，必须**产生流内留痕条目**（现状 R1 只做取消不留痕） | 取代交错场景断言（AC-CHAT-014；解决 Q-CHAT-007） | P0 |
| **FR-CHAT-045** | 破坏性二次确认（`confirm`）迁入流内 `auth` 卡；批准后 `data-decision=approved`、拒绝后 `data-decision=rejected`，操作按钮收起、固化区显示时间戳 | shim D1~D5 | P0 |
| **FR-CHAT-046** | 授权卡含**范围与后果预演** + **审计入口**；已决策授权卡**不可重复决策**；旧卡不消失（两态并存） | shim D1/D4/D6/D7 | P0 |
| **FR-CHAT-047** | **授权记录流内可回看**：批准/拒绝结果在流内固化可读；与工具栏「审计」视图分工（流 = 会话审计线索；审计 = 完整台账） | AC-CHAT-016 | P0 |
| **FR-CHAT-048** | **busy / 回合语义**：多张未答卡是否允许并存、卡内提交是否受 `pending` 门控、未答卡与拾取回合/会话切换的关系，必须有明确且一致的规则（不得出现「回合永远卡在处理中」） | 回合语义规则 + 场景断言（AC-CHAT-014；解决 R-CHAT-007） | P0 |
| **FR-CHAT-049** | **流内留痕零明文边界**：授权卡 / 系统事件行不得把命令参数体、URL query、页面文本等明文带进留痕；留痕字段遵守既有白名单 + URL 去参 | 零明文门禁（AC-CHAT-021；解决 R-CHAT-008） | P0 |

### 5.6 TRACE — 引用卡与系统事件行（叶子 v4-4）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-050** | 引用生命周期在流内以 `ref` 卡呈现（有效/失效）；**失效卡保留**，新引用序号递增 | shim E5 + ref-store 复用断言 | P0 |
| **FR-CHAT-051** | 重拾 / 重锚 **生成新引用卡**（新 id、序号递增）+ **旧引用零改动**（沿用 R3 append-only 先例） | R3 语义回归断言 | P0 |
| **FR-CHAT-052** | 失效卡内含失效原因 + 两条恢复路径（「重新拾取」/「改用描述」，兜底输入默认收起） | shim E2/E3/E4 | P0 |
| **FR-CHAT-053** | **系统事件行**：单行 + `HH:MM:SS` 时间戳 + 只追加；不得被下一次渲染/notice 覆盖 | shim E1 + 无覆盖断言 | P0 |
| **FR-CHAT-054** | **6+ 条瞬时通道归并**：`#env-guard` / `#site-hint` / `#onboarding` / `#discovery-notice` / `#notice` / `#send-reason` 与导航失效、探测态等系统信息，统一以系统事件行/过程卡族承载；既有语义不丢（onboarding/discovery-notice 仍可终结） | 通道归并矩阵断言（AC-CHAT-002 / AC-CHAT-011） | P0 |
| **FR-CHAT-055** | **拾取入口迁移后的可发现性与救援路径重锚**：面板侧拾取入口消失后，页面侧拾取入口（已授权站点）仍可达；未授权站点必须有可发现的拾取/授权引导路径；引用卡的「重新拾取」路径不得复用已失效的 `#l0-pick` | 可发现性 + 救援路径断言（AC-CHAT-012；解决 R-CHAT-012） | P0 |

### 5.7 NEXT — 下一步推荐卡（叶子 v4-4）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-060** | **推荐卡生产者机制**：本 Feature 范围内的推荐内容**从既有真值派生**（L2 计数 / 五类风险 / 引用状态 / 探测状态 / onboarding 步骤），**不新增 LLM 侧产物**；若必须引入 LLM 侧产物则属新面，须显式登记 | 生产者依赖清单 + 无新增 LLM 调用断言（AC-CHAT-013；解决 O-CHAT-006） | P0 |
| **FR-CHAT-061** | **chips 即指令**：推荐 chip 点击直接发起对应回合（不跳到浮层/不复制到输入框） | shim B4 + 点击发起断言 | P0 |
| **FR-CHAT-062** | 推荐**产生规则表 + 优先级 + 上限**（几张推荐卡 / 几个 chip）在实现层落成且可复算；无候选时不得渲染空卡 | 规则表 + 上限断言 + 空态断言（AC-CHAT-013） | P0（规则表细化 P1） |
| **FR-CHAT-063** | **推荐与 `pending` 门控**：`pending` 期间 chip 的可用性有明确规则，且不得破坏 `sendDisabledReason` 语义 | 门控一致性断言（AC-CHAT-014） | P0 |
| **FR-CHAT-064** | 推荐内容不得越过安全边界（不得把被拦/被 deny 的动作作为可点推荐；不得含明文） | 安全反向断言（AC-CHAT-021） | P0 |

### 5.8 DENSITY — 密度新口径与防滥用（叶子 v4-1）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-070** | **新测量根 = 三区外壳**（工具栏 + 状态栏 + 流容器的骨架层）；**流内容不计入默认密度**（法五）；口径以可机器复算的单一来源实现 | 口径单源 + 复算断言（AC-CHAT-009） | P0 |
| **FR-CHAT-071** | **豁免边界仍只认 `hidden`**：`display:none` / `visibility:hidden` / `opacity:0` / `pointer-events:none` / 移出视口 / `aria-hidden` **一律不豁免**（仍计入） | 口径反证（AC-CHAT-009） | P0 |
| **FR-CHAT-072** | **防滥用 ①**：**单张卡内部可点元素 ≤6**（超出 = 拆卡或改为入视图/进程卡族） | 逐卡可点计数断言（AC-CHAT-009） | P0 |
| **FR-CHAT-073** | **防滥用 ②**：**首屏（空流欢迎态）卡片数 ≤2** | 首屏卡片计数断言（AC-CHAT-009） | P0 |
| **FR-CHAT-074** | **阈值哲学保留 + 全部登记格重算**：`7/15`（默认）· `9/20`（首装）· `17/35`（风险）保留；v4 新建密度基线文件（不改写 v3 基线），登记格（9 强制格 + 15 风险格 + worst + 阶段 F 比对格）全部按新布局重算 | 新基线文件 + 登记格复算（AC-CHAT-009） | P0 |
| **FR-CHAT-075** | **防滥用约束必须配可 FAIL 反证**：单卡 ≤6 / 首屏 ≤2 / CSS 隐身不豁免 / 流内容豁免不可被滥用为「把控件藏进流子树来降密度」，逐条配实跑反证 | 反证日志（AC-CHAT-009 / AC-CHAT-023） | P0 |

### 5.9 LEDGER — 取代台账与门禁重定标（内嵌各叶 + 父收口）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-080** | **新建 v4 取代台账**（`docs/v4-supersession-ledger.json`，不改写 v3 台账），在其中**显式登记**「接管 v3 台账的后续取代」；沿用按行判定 + `leafBases` 叶段判据 | 台账存在 + 按行判定门禁 | P0 |
| **FR-CHAT-081** | **受保护门禁区段处置原则**：两段保护段（journey `42766..54004` sha256 `6b45c3fa…` / binding `107780..115930` sha256 `be9ad0e9…`）**逐段决策**（保留 id 兼容 / 显式解除并新 pin）；**任何保护段哈希变更必须台账留痕（old→new + 理由 + 日期），禁静默改写** | 保护段哈希核对 + 台账留痕（AC-CHAT-017） | P0 |
| **FR-CHAT-082** | **journey 保护段显式处置（R-CHAT-002）**：v2 量化红线「流区高度占比 ≥65% / 高度不回退」**语义保留**；「composer 贴底 ∈[0,+8px]」被法则四（无常驻输入框）**显式取代**并走取代台账登记；`getElementById('log')`/`('composer')` 相关断言按新三区等价改写，强度不降 | 取代台账条目 + 等价断言（AC-CHAT-017） | P0 |
| **FR-CHAT-083** | **测试守恒不降**：journey ≥167 / insight ≥108（当前 116）/ binding ≥192 / l0 ≥73 / l1 ≥64 / l2 ≥68 / density ≥60 / sidepanel-view ≥38 / nodeTestRuntime ≥646（当前 832）；取代走台账按行包含判定 + `leafBases` 双段审计沿用 | 计数下界门禁 + 按行判定（AC-CHAT-019） | P0 |
| **FR-CHAT-084** | **元门禁只增不减**：`gate-integrity` 受审集合、`CHROMIUM_GATES.length === 9`、R1a~R4d、`expectFailPattern` 声明下界（≥15）与判定器 selftest 保持并只增 | 元门禁门禁 | P0 |
| **FR-CHAT-085** | **取代台账反证**：故意删除 1 条既有断言 → 取代台账 / 计数下界守卫 **FAIL**；还原后 PASS（实跑留日志） | 反证日志（AC-CHAT-023） | P0 |

### 5.10 VOL — 体积义务 V3-VOL-3（父级收口锚）

| ID | 需求描述 | 验收标准 | 优先级 |
|----|---------|---------|--------|
| **FR-CHAT-090** | **收口重定 sidepanel 体积基线**：F 重构落地收口时重新登记 `SIDEPANEL_BASELINE_BYTES`，按**五要素披露**（前后值 + 日期 + 来源 + 理由 + 历史保留）；容差 5% 公式不变 | 体积门禁 + 五要素登记核对（AC-CHAT-018） | P0 |
| **FR-CHAT-091** | **设绝对值上限（带值闭合）**：置 `PENDING_ABSOLUTE_CAP.resolved = true` 且**同时**给出 `newBaselineBytes` + `absoluteCeilingBytes`（≥ 新基线）+ `resolvedOn`（`YYYY-MM-DD`）；**不得**预填、**不得**静默删除标记 | `evaluatePendingAbsoluteCap()` + 反证（AC-CHAT-018） | P0 |
| **FR-CHAT-092** | **绝对上限数值来源 = 推导规则（不预填先验魔法数）**：收口时按「重构落地**实测值向上取整到 50 KB 档** + **10% 余量**」推导，交**作者一句确认**后闭合 | 推导记录（实测值 + 档位 + 余量 + 作者确认）+ 闭合值（AC-CHAT-018） | P0 |
| **FR-CHAT-093** | **不得设未经要求的自缚装置**（V3-VOL-1 ② 教训）：`SIDEPANEL_CEILING_CAP` 保持 `record-only`，不得重新读取它作为判定；绝对上限不得成为「挡住合法功能」的装置 | 判定器不读取 cap + 登记说明（AC-CHAT-018 / NG-CHAT-007） | P0 |
| **FR-CHAT-094** | **各叶轮内中间重登记**：每个叶子在自身轮次按既有纪律做中间重登记（五要素、容差 5% 不动）；最终抬升点 = 父级收口（由最后完成叶执行） | 各叶重登记核对（AC-CHAT-018） | P0 |

---

## 6. 非功能需求 (NFR)

| ID | 类别 | 需求描述 | 验收标准 |
|----|------|---------|---------|
| **NFR-CHAT-001** | 留痕完备性 | 五类过程（问答 / 授权决策 / 引用生命周期 / 系统事件 / 推荐）在流内条目完备且携带顺序与时间戳；同一事件序列可回放为同一可见流；**不得**出现「解析即从界面消失」的路径 | 留痕完备性矩阵 + 回放等价断言（AC-CHAT-010） |
| **NFR-CHAT-002** | 可用性 | 侧栏 320–560px（窄屏优先）下**零水平溢出**（收起/展开/视图打开/流内长卡各态）；320px 下常驻元素集合 = 400px 下集合（不删元素，只允许文案截断） | UI 门禁（320/400/520 × 900）（AC-CHAT-022） |
| **NFR-CHAT-003** | 性能 | 流渲染**不整树重建**：追加新条目时不得每次全量清空重建；自动滚底与用户上滚回看的冲突遵守既有 `scroll-policy`（`BOTTOM_THRESHOLD_PX = 48`） | 性能断言 + 滚动行为回归（AC-CHAT-010） |
| **NFR-CHAT-004** | 可访问性 | 卡有 `role`（或等价语义）+ `.ts` 时间戳可读；流为 `role=log`；固化态变化有 `aria-live` 语义；键盘/读屏可达顺序合理；收起一律 `hidden` + `aria-expanded`/`aria-controls` 成对；`:focus-visible` 可见；不靠颜色单独传达状态；收起内容不在 tab 序 | 无障碍门禁（AC-CHAT-022） |
| **NFR-CHAT-005** | 安全 | 判定链语义零变化（pin）；零明文（回执 8 字段 + 审计白名单 + URL 去参）；未授权站点零注入；`manifest` 零新增权限；harden 负控保持 | 安全反向断言 + 零注入门禁（AC-CHAT-021） |
| **NFR-CHAT-006** | 体积纪律 | 净增重构的体积须显式登记（五要素）；`content.js` 177,076 B 无容差；`pick-layer.js` 33,900 B 不增长；`sidepanel` 收口闭合绝对上限（不带先验魔法数、不伪闭合、不自缚） | 体积门禁 + 登记核对（AC-CHAT-018） |
| **NFR-CHAT-007** | 测试守恒 | 全部计数下界只增不减；断言只强不弱；取代走台账（按行判定 + `leafBases` 双段）；反证必须实跑且声明 `expectFailPattern`；禁橡皮图章 | 取代台账 + 计数下界 + 元门禁（AC-CHAT-019） |
| **NFR-CHAT-008** | 主题 | **明暗双主题对称成立**：跟随系统 + 手动浅色/深色三态均正确；两主题下所有状态可读且不只靠颜色；主题状态使用**已有** `storage` 权限持久化 | shim G4/G6/G7 + 双主题渲染门禁（AC-CHAT-022） |
| **NFR-CHAT-009** | 工程纪律 | 门禁**严格串行、一次一个 Chromium**（本机内存紧张，历史曾 OOM）；日志完整落盘（禁 tail 截断）；反证 FAIL → 还原 → PASS 全过程留档 | 门禁日志核对（AC-CHAT-023） |
| **NFR-CHAT-010** | 契约同源 | 设计契约（shim 60 断言）机器化 60/60；密度口径与设计稿/新基线**同源可交叉复算**；设计稿数字 ≠ 真实产物数字（分列登记） | shim 实跑 + 口径一致性检查（AC-CHAT-025 / AC-CHAT-009） |
| **NFR-CHAT-011** | 长会话可读性 | 在 `MAX_SESSION_TURNS = 40` 量级下，留痕与可读不冲突：自动滚底 + 可上滚回看 + 系统事件行轻量化；过程卡族可折叠（沿用 480 字符 / 10 行），**固化卡（ask/auth）与系统事件行不压缩**（压缩 = 丢事实）；流内检索本 Feature 不做（非目标） | 长会话场景断言 + 折叠规则登记（EC-CHAT-006） |
| **NFR-CHAT-012** | 持久化边界 | 流事件**至少**内存 append-only + 会话内可回放；持久化范围（是否落 `chrome.storage.session`）由 plan 定，但**不得**把 key/剪贴板/通知/书签正文等敏感明文写入任何存储 | 持久化边界断言 + 零明文（AC-CHAT-021） |

---

## 7. 边界情况 (EC)

| ID | 场景 | 处理方式 |
|----|------|---------|
| **EC-CHAT-001** | `ask` 卡 60 s 超时未答 | 结算为 `canceled` 并**留痕**（固化态「已取消」+ 时间戳）；**不代填默认值**（fail-closed）；后续回合不被永久阻塞 |
| **EC-CHAT-002** | 拾取回合与后台提问交错（R1 现场） | 被取代的提问 settle 为 `canceled` **且产生流内留痕条目**；两张卡按时间顺序保留在流内；不得出现「回合永远卡在处理中」 |
| **EC-CHAT-003** | 会话切换（`history` 动作） | 过程事实不得静默丢失：工具卡元数据（`tool`/`ok`/`ms`）与过程条目保留或显式登记截断规则；被替换的旧会话事件不得读成「从未发生」 |
| **EC-CHAT-004** | 320px 窄屏下流内长卡（长 URL / 长代码 / 长引用文本） | 零水平溢出；长内容按既有折行/截断口径处理；常驻元素不删；可读性优先于完整展示（详情可展开） |
| **EC-CHAT-005** | 引用选择器断链但目标文字仍可见（R3 现场） | 引用判失效（fail-closed）→ 系统事件行说明 + 失效卡保留 + 「重新拾取 / 改用描述」；重锚生成新引用（新序号），旧引用零改动 |
| **EC-CHAT-006** | 长会话（接近 `MAX_SESSION_TURNS = 40`）流很长 | 自动滚底 + 上滚回看；过程卡族可折叠；系统事件行轻量单行；固化卡不压缩；本 Feature 不做流内检索（显式登记为后续面） |
| **EC-CHAT-007** | 未授权站点：面板侧拾取入口消失 | 必须有可发现的授权/拾取引导路径（法则六：授权入口在设置视图内可发现）；未授权站点**零注入**（不得为了拾取而注入） |
| **EC-CHAT-008** | 推荐生产者无候选（空态） | **不渲染空推荐卡**；不得渲染「下一步：无」之类的假推荐；首屏（空流欢迎态）卡片数仍 ≤2 |
| **EC-CHAT-009** | 固化卡的键盘 / 读屏可达性回归 | 固化后表单收起，焦点移到固化区或下一个合理位置；`aria-live` 播报固化结果；收起内容不在 tab 序；`:focus-visible` 可见 |
| **EC-CHAT-010** | 打开 L2 视图时风险 chips 并存 | 状态栏与 chips 容器仍可见（不因视图替换而 `hidden`）；返回流后风险 chips 仍可见（永不折叠语义） |
| **EC-CHAT-011** | `sidepanel.js` 超过收口后的绝对上限 | 门禁 FAIL（`absoluteCeilingBytes` 判定）；要么收敛体积，要么走显式重登记（须作者裁决，不得静默放宽） |
| **EC-CHAT-012** | 受保护门禁区段哈希漂移 | 取代台账门禁 FAIL；必须登记 old→new + 理由 + 日期后重新 pin；**禁静默改写** |
| **EC-CHAT-013** | 某叶无法在同一叶内完成取代（中间态红灯） | 必须**先缩小该叶范围并显式登记**，不得把红灯遗留给下一叶（每叶收尾全门禁绿纪律） |
| **EC-CHAT-014** | 主题切换持久化失败（`storage` 不可用/写失败） | 降级为「跟随系统」且不阻断界面；不得因主题状态读写失败而影响主流程；按钮态与文案自洽 |

---

## 8. 验收标准（总体验收清单）

> 验收编号独立成组 `AC-CHAT-###`，可被下游 plan / tasks / validate 直接引用。凡标注「门禁」的条目必须能**真 FAIL**（配反证，NFR-CHAT-007）。AC 与设计契约（shim 60 断言）的逐条映射见附录 A。

### 8.1 设计契约（60 断言条款化）

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-001** | **三区结构与语义**：`#region-toolbar`（`header[role=toolbar]`）/ `#region-stream`（`main` 含 `ol#stream[role=log]`）/ `#region-statusbar`（`footer[role=contentinfo]`）齐备且顺序为 toolbar → stream → statusbar；消息流本体为 `ol[role=log]`、正序追加 | 结构门禁（对应 shim A1~A6） |
| **AC-CHAT-002** | **7 主类 + 过程卡族全覆盖**：7 主类（`ai`/`user`/`nextstep`/`askuser`/`auth`/`system`/`ref`）样例全部存在且挂在流 `li` 上；过程卡族 5 形态（工具卡/命令行/思考指示/错误条目/工具通知）全部存在并保留既有信息（工具卡含工具名/ok/ms/预览/折叠记忆） | 结构 + 形态矩阵门禁（shim B1/B2/H11 + 过程族逐项） |
| **AC-CHAT-003** | **固化契约统一**：操作前（表单可见、固化区 `hidden`）→ 操作后（`data-*` 状态、表单收起、固化区显示、`HH:MM:SS` 时间戳、`.card-fixed`）——ask（答/取消）与 auth（批准/拒绝）逐态成立；已固化卡不可二次操作 | UI 门禁（shim C5~C10 / D2~D6） |
| **AC-CHAT-025** | **设计契约机器化**：`option-f-shim.mjs` **60/60 passed**（零失败）；**60 条断言逐条映射到本规范条款**（附录 A）；真实产物侧按条款复算；设计稿契约变更（如 `CARD_TYPES` 扩展）必须登记而非静默改断言 | shim 实跑 + 映射表核对 |

### 8.2 五法则 + 法则六

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-004** | **法一（可静态核验）**：工具栏与状态栏内**不存在** ask-user / 授权 / 推荐类一次性交互；一切一次性交互只出现在流内卡 | 静态核验门禁（shim F6） |
| **AC-CHAT-005** | **法二**：卡片只固化不撤销；**不存在**「撤销回答 / 撤销批准」控件；撤销操作生成**新的系统事件行**并留痕 | 控件不存在 + 事件行断言（shim C9/D6） |
| **AC-CHAT-006** | **法三 + 法六**：工具栏只放常驻导航与（只读）站点摘要、状态栏只放常驻状态与风险、一切过程进流；站点授权/撤销/会话/隐私/知情同意/自动授权等管理操作**落在设置/审计视图**且**未授权首装路径可发现** | 三区归属核验 + 首装可发现性（shim F6 + 视图断言） |
| **AC-CHAT-007** | **法四（输入即 next）**：自由文本输入是流内 next 的一个选项；**流外零输入面**（`#composer`/`#input`/`#send` 三 id DOM 零命中，非 `hidden`） | UI 门禁（shim F7 / C4 + `test/law4-input-as-next.test.ts` L4-1~6） |
| **AC-CHAT-008** | **法五 + 工具栏准入**：工具栏可点 ≤5；工具栏 + 状态栏合计（默认屏 S1）≤7；站点摘要**只读**（非 button/a/input，不计入预算）；4 视图入口带计数徽标 | 密度门禁（shim F1/F2/F4/F5） |

### 8.3 留痕与固化

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-010** | **留痕完备性 + 可回放**：问答 / 授权决策 / 引用生命周期 / 系统事件 / 推荐 五类过程在流内条目完备；同一事件序列可回放为同一可见流；`history`（会话切换）不丢过程元数据；**不存在**「解析即置 `null` 从界面消失」的路径 | 模型门禁 + 回放等价 + 会话切换回归 |
| **AC-CHAT-011** | **系统事件行**：单行 + `HH:MM:SS` 时间戳 + 只追加；同屏 ≥3 条可区分；不被下一次渲染/notice 覆盖；6+ 瞬时通道归并后既有语义不丢 | UI 门禁（shim E1）+ 通道归并矩阵 |
| **AC-CHAT-012** | **引用卡生命周期**：失效卡保留 + 失效原因 + 「重新拾取 / 改用描述」；重拾生成新引用序号递增、旧卡零改动；面板侧拾取入口消失后页面侧/未授权路径仍有可发现入口 | UI 门禁（shim E2/E3/E4/E5）+ 可发现性断言 |
| **AC-CHAT-013** | **推荐卡**：`nextstep` 卡含可点 chips 且点击**直接发起回合**；推荐从既有真值派生（无新增 LLM 调用）；无候选不渲染空卡；规则表 + 优先级 + 上限可复算 | UI 门禁（shim B4）+ 生产者依赖清单 |
| **AC-CHAT-014** | **回合语义**：`supersededAsk` 升级为「取消 + 留痕」；`pending` 门控与未答卡/推荐 chip 关系一致；不出现「回合永远卡在处理中」 | 场景门禁（拾取×后台提问交错 + pending 组合） |
| **AC-CHAT-015** | **超时与取消留痕**：ask 60 s 超时 → `canceled` 留痕、不代填默认值；用户主动取消同样留痕 | 场景门禁（shim C10 + 超时用例） |
| **AC-CHAT-016** | **授权回看**：批准/拒绝在流内固化可读 + 含时间戳 + 审计入口；**站点级授权动作**在设置视图可发现与可读；与审计视图分工（流 = 会话审计线索，审计 = 完整台账） | UI 门禁（shim D1/D4/D7）+ 视图断言 |

### 8.4 密度与防滥用

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-009** | **密度新口径 + 防滥用（可复算）**：① 测量根 = 三区外壳，流内容不计入默认密度；② 豁免边界只认 `hidden`（CSS 隐身不豁免）；③ **单卡内部可点元素 ≤6**；④ **首屏（空流欢迎态）卡片数 ≤2**；⑤ 阈值 `7/15 · 9/20 · 17/35` 保留，v4 新基线登记格（9 强制格 + 15 风险格 + worst + 阶段 F 比对格）全部重算；⑥ 上述 ③④⑤ 各配**可 FAIL 反证**（含「把控件藏进流子树降密度」的滥用反证） | 新密度门禁（三档 × 三视口）+ 反证日志 |

### 8.5 取代台账与门禁重定标

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-017** | **journey 保护段处置留痕**：v2「流区高度占比 ≥65% / 高度不回退」语义保留；「composer 贴底 ∈[0,+8px]」被法四显式取代并登记台账；`journey` 保护段（`42766..54004`）与 `binding` 保护段（`107780..115930`）逐段决策；**任何保护段哈希变更 old→new + 理由 + 日期台账留痕**；等价断言强度不降 | 取代台账门禁 + 哈希核对 + 等价断言 |
| **AC-CHAT-019** | **测试守恒不降**：journey ≥167 / insight ≥108 / binding ≥192 / l0 ≥73 / l1 ≥64 / l2 ≥68 / density ≥60 / sidepanel-view ≥38 / nodeTestRuntime ≥646；取代按行判定 + `leafBases` 双段审计；元门禁只增；替代反证（删 1 条断言 → FAIL → 还原 → PASS） | 取代台账门禁 + 计数下界 + 元门禁 |
| **AC-CHAT-023** | **门禁纪律**：严格串行（无并发 Chromium）、日志完整落盘（无 tail 截断）；反证必须实跑且声明 `expectFailPattern`；FAIL → 还原 → PASS 全过程留档；**每叶收尾全门禁绿**（禁止红灯中间态遗留） | 门禁日志核对 + 反证日志 |

### 8.6 体积、安全与纪律

| ID | 验收标准 | 门禁 / 方式 |
|----|---------|------------|
| **AC-CHAT-018** | **V3-VOL-3 带值闭合**：收口时 `PENDING_ABSOLUTE_CAP.resolved = true` 且 `newBaselineBytes` / `absoluteCeilingBytes`（≥ 新基线）/ `resolvedOn`（`YYYY-MM-DD`）三值齐备；新基线按五要素披露；绝对上限按推导规则（实测值向上取整到 50 KB 档 + 10% 余量）经作者确认；`resolved:false` 期间**不得预填**；标记**不得**静默删除；`SIDEPANEL_CEILING_CAP` 保持 `record-only` 不被读取 | `size-ruling-vol3` 门禁 + `evaluatePendingAbsoluteCap()` + 反证 |
| **AC-CHAT-020** | **不动面零 diff**：`dist/content.js` 177,076 B、`dist/pick-layer.js` 33,900 B 逐字节不变；`manifest.json` 零 diff（零新增权限、无 `contextMenus`、无静态 `content_scripts`）；判定链 `policy.ts`/`auto-authorize.ts` sha256 = pin；`KIND_SET` 零 diff；`packages/web-cli-base/**` 零 diff；`package.json` 依赖零新增；上游 v1/v2/v3 SDDU 目录零 diff | 零 diff / 哈希核对 + 体积门禁 |
| **AC-CHAT-021** | **安全与零明文**：判定链语义零变化；`evaluate` 仍 deny / 未授权 origin 仍 deny / 未知 risk 仍 deny / 破坏性子命令仍 ask / `clipboard read` 仍 ask / `bookmarks remove` 仍 ask；零审计明文（通知/剪贴板/书签）+ URL 去参；流内留痕遵守同一零明文白名单；伪造 `sendMessage` 不能突破 clamp；未授权站点零注入（27 断言保持） | 安全门禁（反向断言）+ 零注入门禁 |
| **AC-CHAT-022** | **窄屏 + 双主题 + 无障碍**：320/400/520 × 900 零水平溢出（各态）；明暗双主题 + 跟随系统三态正确且所有状态可读、不只靠颜色；`aria-expanded`/`aria-controls` 成对；收起用 `hidden`；`:focus-visible` 可见；流 `role=log` + 卡 `role`/时间戳可读；320px 常驻元素集合 = 400px | UI 门禁（三宽度 × 双主题）|
| **AC-CHAT-024** | **人工面如实登记**：人工面清单（观感 / 体感 / 真实权限弹窗 / 多显示器 / 高 DPI / 键盘真实体感等）逐项标注「未执行」或「PASS」，**不得冒充 PASS**；未跑的门禁标注未跑原因 | 文档核对 |

---

## 9. 密度新口径（机器可复算定义）与防滥用

> 本节是本 Feature 的核心口径变更（R-CHAT-004 / FR-CHAT-070~075）。**注意：这是一次口径变更，不是放宽**——法五把「聊天流内容」从**默认密度**中豁免，同时新增两条防滥用约束（单卡 ≤6 / 首屏 ≤2）作为补偿控制。

### 9.1 测量根与豁免边界

| 项 | 新口径 | 与 v3 旧口径的关系 |
|---|--------|-------------------|
| **测量根** | **三区外壳**：工具栏（`#region-toolbar`）+ 状态栏（`#region-statusbar`）+ 流容器的**骨架层** | 旧 = `document.body`（v3 C1 `measurementRoot`） |
| **流内容豁免** | **流内容子树不计入默认密度**（法五：流是记录，不是控件预算）；豁免子树以**单一来源**（口径实现文件）声明，不得分散写死 | 旧：无此豁免 |
| **豁免边界（继承）** | **只认 `hidden` 属性**：`display:none` / `visibility:hidden` / `opacity:0` / `pointer-events:none` / 移出视口 / `aria-hidden` **一律不豁免**（仍计入） | 旧 C1 边界**完全保留**（不放松） |
| **C1~C4 口径** | C1 可点元素 / C2 可见正文行 / C3 可见文本块 / C4 常驻分区——定义沿用 v3 口径，**统计根与外层豁免范围改变**；C3/C4 只登记不上限 | 定义继承，范围重定标 |
| **C2 行宽常数** | `34`（320px、13px 字号）**保留** | 不变 |
| **设计稿与真实产物分列** | 设计稿 shim 数字 ≠ 真实产物数字，**分列登记**（v3 A-UI-001 教训） | 不变 |

### 9.2 阈值（保留 v3 阈值哲学）

| 档 | 状态定义 | C1 可点元素 | C2 可见正文行 | 视口 |
|---|---|:--:|:--:|---|
| **默认态** | 已绑定 + 已授权 + 探测完成 + 无待确认 + onboarding/discovery-notice 已终结 | **≤ 7** | **≤ 15** | 320 / 400 / 520 |
| **首装态** | onboarding 或 discovery-notice 生效中 | **≤ 9** | **≤ 20** | 320 / 400 / 520 |
| **风险态** | 五类风险任一生效 | **≤ 17** | **≤ 35** | 320 / 400 / 520 |
| **全展开态**（对照） | 全部视图/展开层打开 | 只登记 | 只登记 | — |

> 阈值**只允许收紧**；放宽必须作者/编排器裁决并显式登记（前后值 + 日期 + 理由 + 历史保留），且不得以放宽上限换功能（NG-CHAT-004）。

### 9.3 防滥用约束（新增，必须配可 FAIL 反证）

| # | 约束 | 理由 | 反证 |
|---|------|------|------|
| **A1** | **单张卡内部可点元素 ≤6** | 防止「把过程控件塞进一张流内卡」绕过默认密度 | 在某卡内注入第 7 个可点元素 → FAIL |
| **A2** | **首屏（空流欢迎态）卡片数 ≤2** | 防止「用流内卡堆满首屏」绕过默认密度 | 首屏注入第 3 张卡 → FAIL |
| **A3** | **流内容豁免不可被滥用**：不得把**本应常驻的导航/管理控件**移入流子树以降低外壳密度；豁免子树范围由口径单源声明且**不得**在实现中扩大 | 防止「把控件藏进流子树来降密度」 | 把工具栏某入口移入流子树 → 归属判据 FAIL |
| **A4** | **CSS 隐身不豁免**（继承 v3 RP-V3-03） | 防止用 `display:none`/`visibility:hidden` 刷低密度 | 改为 CSS 隐身 → 计数不下降，若下降即 FAIL |

### 9.4 交付与登记

- **v4 新建** `docs/v4-density-baseline.json`（新口径 + 新登记格），**不改写** v3 基线；在新文件中登记「与 v3 基线的差异与理由」（换口径重定标，非放宽）。
- 复用密度门禁的**实现骨架与反证判定器**；豁免边界与登记格重算；**9 强制格 + 15 风险格 + worst + 阶段 F 比对格**全部按新布局重算。
- 首轮真实产物复测必须登记为基线（三档 × 三视口）+ 与设计稿数字分列。

---

## 10. 取代台账与 journey 保护段处置

> 背景：v3 末轮门禁计数 journey 167 / insight 116 / l0 164 / l1 103 / l2 71 / density 127；F 重构会移除 `#l0-decision`、`#l0-statusbar`、`#l2-entries`、`#risk-rail`、`#l0-pick` 等大量 DOM，**删除行远多于 v3**（R-CHAT-001 / R-CHAT-015）。纪律 = **零删除、零降级**（取代必须走台账且计数不减）。

### 10.1 处置原则

| 门禁 | 处置原则（本 Feature 强制） |
|------|--------------------------|
| `test/ui/journey.mjs`（167） | **保护段显式处置**（§10.2）；非保护段：同编号 old→new 改写 + 台账登记，总数 ≥167，禁删除/降级 |
| `test/ui/insight.mjs`（116，下界 108） | 新断言落新增门禁文件；被取代旧条目逐条登记 old→new；`count ≥ 108` |
| `test/ui/binding.mjs`（192） | 既有编号零删改（`#21*`/`#22*`）；新断言追加新编号；`count ≥ 192`；保护段（`#22a~#22l`）逐段决策 |
| `test/ui/l0.mjs` / `l1.mjs` / `l2.mjs` | **整文件重写风险面**：L0 骨架 / L1 入口机制 / L2 入口位置被取代 → 逐条登记，叶下界只增不减（73 / 64 / 68） |
| `test/ui/density.mjs`（127） | **换口径重定标**（§9）；保留原文件为 v3 历史，v4 新基线；叶下界 ≥60 |
| `test/sidepanel-view.test.ts`（38） | 布局/DOM 契约随迁移等价改写并逐条登记；用例数 ≥38；4 项布局契约逐项仍被断言（可等价或更强） |
| `test/supersession-ledger.test.ts` + `test/gate-integrity.test.ts` | **只增不减、只强不弱**（元门禁不可失效） |

### 10.2 journey 保护段显式处置（R-CHAT-002）

| 断言组 | 现状 | **处置** |
|--------|------|---------|
| 保护段 `42766..54004`（`#15a~#15q`，sha256 `6b45c3fa…`） | 逐字读取 `getElementById('log')` / `('composer')`，按 `getComputedStyle` 断言 `#log flex-grow=1`、布局高度占比、composer 贴底 | **语义保留 + 等价改写**：① 「流区高度占比 ≥65% / 高度不回退」（v2 `AC-V2-002`/`FR-V2-023`）**语义保留**，按新三区（`#region-stream`/`#stream`）等价改写；② 「composer 贴底 ∈[0,+8px]」被**法四（无常驻输入框）显式取代**——登记取代台账（old 断言 → new 断言「默认屏无可见常驻输入框」）；③ 保护段哈希**变更必须台账留痕**（old→new sha256 + 理由 + 日期），**禁静默改写** |
| 保护段 `107780..115930`（`#22a~#22l`，sha256 `be9ad0e9…`） | V2-3 R2 command-level override chain（后台/协议面） | **逐段决策**：属后台/协议面（高存活），默认**保留 id 兼容**；若因 UI 承载变更确需改动，同样走台账 old→new + 哈希留痕 |

### 10.3 每叶收尾全门禁绿

- 4 个叶子**逐个**完成，每叶收尾必须跑完全部门禁（**串行**）且全绿，才允许下一叶开工（AC-CHAT-023）。
- 禁止把未取代的红灯遗留给下一叶（EC-CHAT-013）；若某叶无法在同一叶内完成取代，**必须**先缩小该叶范围并显式登记。
- **未采纳**把「取代台账 + 密度重定标」独立成第 5 叶：台账/密度重定标必须与它取代的 DOM **同步落地**，独立成叶无法独立全绿（discovery §7.6）。

### 10.4 体积义务的落位（V3-VOL-3）

| 事项 | 落位 |
|------|------|
| 单轮五要素披露（中间重登记） | **各叶**在自身轮次内执行；容差 5% 公式不动 |
| 重定基线 + 设绝对上限（`PENDING_ABSOLUTE_CAP` 带值闭合） | **父级收口锚**；由**最后完成的叶（预计 v4-4）**在其收口轮执行；数值按 §5.10 推导规则 + 作者确认 |

---

## 11. 子 Feature 拆分与交付顺序

> 结构 = **父 Feature（轻量规范容器）** + **4 个叶子子 Feature**（depth=2，每个叶子可独立走 SDDU 全流程；父不执行 build / review / validate）。目录名沿用 v2/v3 风格（`specs-tree-` 前缀 + **直挂父目录**，无 `children/` 中间层）。

| 叶子 | 目录 | 覆盖问题 | 一句话范围 | 承载 FR / AC |
|------|------|---------|-----------|-------------|
| **v4-1 三区骨架与密度重定标** | `specs-tree-v4-1-zone-shell-density` | Q-CHAT-004 / 005（部分）/ 008 + R-CHAT-004/005/011 + D7③ | 三区骨架落定（工具栏 ≤5 + 站点摘要只读 + 4 视图入口 + 主题切换）；状态栏常驻（连接状态一行 + 风险 chips，永不折叠语义新判据）；**密度新口径 + 防滥用 + 反证**；替换/入口迁移 | FR-CHAT-010~017 / 070~075；AC-CHAT-004~009 / 022 |
| **v4-2 聊天流 append-only 事件模型与卡渲染** | `specs-tree-v4-2-chat-stream-model` | Q-CHAT-001（模型层）/ 005 / 007 + R-CHAT-006/013/014 + O-CHAT-002/008 | 流事件模型（不可变、单调、可回放）；7 主类 + 过程卡族渲染与固化契约；tool/command/thinking/error/notice 归位；滚动跟随/回看与 320px 换行 | FR-CHAT-020~037；AC-CHAT-001~003 / 010 / 011 / 025 |
| **v4-3 ask-user / 授权卡流内化 + 留痕固化** | `specs-tree-v4-3-ask-auth-inflow` | Q-CHAT-001（承载层）/ 002 / 007 + R-CHAT-007/008 + O-CHAT-005 | `ask-user`（choice/text）与破坏性确认从独占槽迁入流内 + 固化不可逆 + 60s 超时/取消留痕；`supersededAsk` 升级为「取消 + 留痕」；授权记录流内可回看；零明文边界 | FR-CHAT-040~049；AC-CHAT-003 / 014 / 015 / 016 / 021 |
| **v4-4 引用卡 / 系统事件行 / 推荐卡 + 拾取入口迁移** | `specs-tree-v4-4-ref-system-nextstep` | Q-CHAT-003 / 005 / 006 / 013 + R-CHAT-012 + O-CHAT-006 | 引用卡（有效/失效 + 新序号递增 + 失效卡保留 + 重拾/重锚留痕）；系统事件行（单行 + 时间戳 + 只追加，6+ 通道归并）；推荐卡（真值派生 + chips 即指令）；拾取入口迁移后的可发现性与救援路径重锚 | FR-CHAT-050~064；AC-CHAT-012 / 013 / 016；**V3-VOL-3 收口锚执行** |
| **父 Feature（轻量规范容器）** | 本目录 | — | 聚合 spec/plan 与收口总账（`closeout.md`）；**不承接** tasks/build/review/validate（v2/v3 先例：父 `phase=tasked` / `workflow=4.tasks` / `agent=sddu-tasks`） | — |

### 11.1 依赖与交付顺序

```
v4-1（三区骨架 + 密度重定标）
   │  ← 首个开工；产出「三区骨架 + 密度新口径可测基线」
   ▼
v4-2（append-only 事件模型 + 卡渲染）  ← 依赖 v4-1 的三区容器承载流
   │  ← 地基：v4-3 / v4-4 的事件模型与固化契约来源
   ├──────────────────────────────┐
   ▼                              ▼
v4-3（ask/授权流内化）          v4-4（引用/系统/推荐 + 拾取迁移）
   └──────────────┬───────────────┘
                  ▼
        父级收口（V3-VOL-3 带值闭合由最后完成叶执行）
```

| 顺序 | 叶子 | 前置 | 独立性说明（**能独立做到全门禁绿**） |
|:--:|------|------|-----------------------------------|
| 1 | v4-1 | 无（首个） | 骨架先行，其余叶在其上叠加；密度新口径一旦落地即成共同基线；只改侧栏骨架 + 新增/重定标密度门禁 |
| 2 | v4-2 | v4-1（三区容器 + 密度基线） | 纯模型 + 渲染层，不依赖 SW 契约变更（可先用既有 5 条通道喂数据） |
| 3 | v4-3 | v4-2（事件模型 + 卡渲染） | 依赖 v4-2 的模型与卡渲染，但**不依赖** ref/system 两类卡的迁移 |
| 4 | v4-4 | v4-2（事件模型 + 卡渲染）；建议在 v4-3 之后串行门禁 | 依赖 v4-2；与 v4-3 无强耦合（可并行分解，门禁串行）；**父级 V3-VOL-3 收口锚在其收口轮执行** |

> **跨叶硬义务（V3-VOL-3）落位**：① 作为**父级收口验收锚**（本 spec §5.10 / AC-CHAT-018）；② 由**最后完成的叶（预计 v4-4）**在其收口轮执行「重定基线 + 设绝对上限 + 更新 `test/size-baseline.ts`」；③ 各叶在自身轮次内按五要素做**中间重登记**（容差 5% 不动）。

---

## 12. 裁决记录（O-CHAT-001~009 逐条）

> 来源：编排器 2026-09-18 裁决（作者已授权全流程自行调度）+ 本 spec 对未明确裁决项的补充处置。逐条含**理由**；凡「留给 plan」者标注不阻塞本 spec。

| ID | 事项 | **裁决 / 处置** | 理由 | 落点 |
|----|------|----------------|------|------|
| **O-CHAT-001** | 系统事件行折叠阈值 / 已闭合卡压缩 / 流内检索（设计稿自认留给重构） | **部分裁决**：① 过程卡族折叠阈值**沿用既有 480 字符 / 10 行**（单一来源，不得为每卡型各写一套）；② **已闭合固化卡（ask/auth）与系统事件行不压缩**——压缩 = 丢事实，违反法二；③ **流内检索本 Feature 不做**（显式登记为后续面，非目标）；长会话可读性由「自动滚底 + 上滚回看 + 系统事件行轻量化 + 过程卡族折叠」承载 | 法二「留痕即事实」优先于长会话便利；检索需新面（索引/命中高亮），超出「承载面迁移」范围 | NFR-CHAT-011 / EC-CHAT-006 / FR-CHAT-032 / FR-CHAT-035 |
| **O-CHAT-002** | append-only 事件模型与引用注册表的边界（id 口径 / 会话切换 / 持久化范围） | **裁决**：① **id 口径统一 `nextId` 单调序列**（引用仍用 `ref_<n>` 业务序号，但流条目 id 走统一序列）；② **流内 `ref` 卡 = 引用注册表的投影 + 事件记录**（`ref-store` 仍是引用真值源；卡记录「投影点」，重锚产生新投影点）；③ **会话切换保留过程事实**（不得丟工具卡元数据；截断规则须显式登记，禁静默）；④ **持久化范围**（是否落 `chrome.storage.session`）**留给 plan**（至少内存 append-only + 会话内可回放） | 统一 id 避免「事件 id 与引用序号两套语义」；ref-store 已是 append-only 先例（R3）；持久化涉及存储配额与零明文边界，属技术选型 | FR-CHAT-020 / FR-CHAT-024 / FR-CHAT-050 / FR-CHAT-051 / EC-CHAT-003 |
| **O-CHAT-003** | 工具栏常驻导航准入规则（置换 + 计数徽标 + 主题切换） | **裁决**：① **准入规则** = 常驻入口必须「与单次会话无关」且属于「四视图 / 主题 / 站点摘要」；② **满额 5 时置换规则** = 新增入口必须显式取代（登记 old→new + 理由），禁止静默第 6 个；③ 4 视图入口**带计数徽标**（复用 `l2/counts.ts` 计数单源）；④ **主题切换为新增控件**（三态：跟随系统 / 浅色 / 深色），状态用**已有 `storage` 权限**持久化；⑤ 站点摘要**只读**，不计入预算 | 法三/法五；预算语义从「花在一次性交互」反转为「花在常驻导航」；主题三态是 F 稿设计契约（shim G4/G6/G7） | FR-CHAT-011 / FR-CHAT-016 / NFR-CHAT-008 |
| **O-CHAT-004** | 体积绝对上限的数值来源 | **裁决（编排器）**：spec 记录**推导规则**而非预填值（`PENDING_ABSOLUTE_CAP` 机制禁止在 `resolved:false` 时预填）——收口时按「**重构落地实测值向上取整到 50 KB 档 + 10% 余量**」推导，交**作者一句确认**后闭合；**不设先验魔法数**；`SIDEPANEL_CEILING_CAP` 保持 `record-only`（V3-VOL-1 ② 教训） | 预填即伪闭合（判定器 FAIL）；绝对上限需独立于 5% 公式，且不得成为自缚装置 | FR-CHAT-091 / FR-CHAT-092 / FR-CHAT-093 / AC-CHAT-018 |
| **O-CHAT-005** | 站点级授权 / 撤销 / 会话与分组 / 隐私开关 / 知情同意 / 自动授权开关的归属 | **裁决（编排器，法则六）**：「**过程入流，管理入视图**」——上述**与单次会话无关的管理操作**归工具栏视图（设置 / 审计）；**未授权首装路径的授权入口**必须在设置视图内可发现；工具栏站点摘要保持只读；**不新增第 8 类卡承载站点级授权**（它没有会话过程语义） | 法三「三区各司其职」的缺口补全；过程（有会话上下文）与管理（无会话上下文）是两类交互 | FR-CHAT-016 / AC-CHAT-006 / EC-CHAT-007 |
| **O-CHAT-006** | 「下一步推荐」的内容产生规则 | **裁决**：① 前提 = **从既有真值派生**（L2 计数 / 五类风险 / 引用状态 / 探测状态 / onboarding 步骤），**不新增 LLM 侧产物**；② **规则表 + 优先级 + 上限**由 plan 落成（需求层定「chips 即指令 + 上限 + 无候选不渲染」）；③ chip 点击语义 = **直接发起回合**；④ 与 `pending` 门控关系须一致（FR-CHAT-063）；⑤ **若必须引入 LLM 侧产物 → 属新面**，显式登记、不在本 Feature 隐含 | 「推荐」是本 Feature 唯一需新增生产者的内容类型；新增 LLM 调用会引入网络/隐私/成本新面 | FR-CHAT-060~064 / AC-CHAT-013 / EC-CHAT-008 |
| **O-CHAT-007** | 风险位形态（顶部 rail → 底部 chips）是否合规「永不折叠」及新机器判据 | **裁决**：「永不折叠」= **语义约束**（可发现 + 不被任何展开/收起/视图切换影响 + 常驻可见），**不绑定「必须在顶部」实现位置**；**新机器判据** = 状态栏本体无 `hidden` + chips 容器可见 + 打开任意视图后 chips 仍可见（shim H5/H8）+ 密度风险类归属重定标 + 祖先闭包探针；五类风险 chips **有则显示、无则不占行**；chip 详情展开不占默认密度 | 位置/形态是实现自由度，语义是红线；只有把语义机器化才能既允许 F 稿形态又保住不折叠 | FR-CHAT-004 / FR-CHAT-013 / FR-CHAT-017 / AC-CHAT-007 / AC-CHAT-022 |
| **O-CHAT-008** | F 的 7 类卡与现状 tool/command/thinking/error 的关系 | **裁决（编排器）**：**7 主类 + 过程卡族**——F 稿 7 类为「主类」；现状 5 种既有过程形态（工具卡 / 命令行 / 思考指示 / 错误条目 / 工具通知）归并为「**过程卡族**」（AI 消息族子形态），**全部 append-only 留痕、适用固化契约**；**不许丢弃任何既有过程形态**；`CARD_TYPES` 若扩展须**登记设计契约变更**（不静默改 shim） | 工具卡是 TASK-023 既有能力（工具名 + ✓/✖ + 耗时 + 预览 + 折叠记忆），并入 `ai`/`system` 会丢信息与门禁 | FR-CHAT-021 / FR-CHAT-035 / FR-CHAT-036 / AC-CHAT-002 / AC-CHAT-025 |
| **O-CHAT-009** | 外部竞品事实的补齐 | **裁决（编排器）**：设计已由作者定向（F 稿定稿），竞品调研**非阻塞**，登记为「**未执行，不阻塞**」——**不编造任何竞品结论**；解除条件 = 提交 `latest_public_web → doubao_search` 计划后补齐，或作者直接提供事实 | 设计基准已定；编造竞品结论违反事实纪律 | §13 开放问题（open，非阻塞） |

---

## 13. 开放问题

| # | 问题 | 状态 | 处置 |
|---|------|:--:|------|
| 1 | 流事件的**持久化范围**（是否落 `chrome.storage.session`；配额与零明文边界） | 待决策 | **留给 plan**（需求层已定：至少内存 append-only + 会话内可回放 + 零明文；O-CHAT-002④） |
| 2 | **推荐产生规则表**的具体条目、优先级与上限数值 | 待决策 | **留给 plan**（需求层已定规则：真值派生 + chips 即指令 + 无候选不渲染；O-CHAT-006） |
| 3 | 新密度口径的**实现载体**（哪个门禁文件 / 如何复用 Chromium 运行时测量 / 豁免子树的单源声明方式） | 待决策 | **留给 plan**（需求层已定口径 §9 与「新增文件、不改写 v3 基线」） |
| 4 | `docs/v4-supersession-ledger.json` 与 `docs/v4-density-baseline.json` 的**文件名与门禁挂载点** | 待决策 | **留给 plan**（需求层已定 schema 纪律与不覆盖 v3 历史） |
| 5 | **过程卡族的具体 DOM/类名契约**（`tool`/`command`/`thinking`/`error`/`notice` 的标记方式） | 待决策 | **留给 plan**（需求层已定：全形态保留 + 适用固化契约 + 可机器识别） |
| 6 | 「未答卡并存」的**具体上限与仲裁规则**（多张未答卡与 `pending` 的组合空间） | 待决策 | **留给 plan / v4-3**（需求层已定：规则必须明确一致、不得永久卡住；FR-CHAT-048） |
| 7 | **外部竞品调研**（O-CHAT-009） | 待调研（**非阻塞**） | 下一回合可提交 `latest_public_web → doubao_search` 计划补齐，或作者直接提供；**不得**据此编造结论 |
| 8 | 页面侧是否提供**流内「下一步」推荐**所需的数据通道（现有 5 条通道是否足够） | 待决策 | **留给 plan / v4-4**（若需新增 kind，必须走独立校验器，不进 `KIND_SET`） |

---

## 14. 附录 A：设计契约（60 断言）→ 条款映射

> `option-f-shim.mjs` 60 断言逐条映射到本规范条款。**映射是双向契约**：实现须逐条满足；shim 变更（新增/改写断言）必须登记设计契约变更（FR-CHAT-036 / AC-CHAT-025）。

| 断言组 | 断言 | 契约条款 | 承载 FR / AC |
|---|---|---|---|
| A（6） | A1 三区结构存在；A2 三区顺序；A3 工具栏 `header[role=toolbar]`；A4 流容器含 `ol[role=log]`；A5 状态栏 `footer[role=contentinfo]`；A6 消息流为 `ol[role=log]` 正序追加 | 三区骨架 + 语义 | FR-CHAT-010 / AC-CHAT-001 |
| B（4） | B1 7 类卡样例齐全；B2 每类卡挂在 `#stream` 的 `li`；B3 AI 卡富文本；B4 推荐卡含 chips | 7 主类契约 | FR-CHAT-030~034 / AC-CHAT-002 / AC-CHAT-013 |
| C（11） | C1 text 卡内输入框；C2 choice 卡选项 ≥3；C3 取消入口 + 不代填说明；C4 末项「其他…（我来描述）」兜底默认收起；C5~C8 操作前/后固化（`data-answered` / 表单收起 / 固化文案 / 时间戳）；C9 不可二次回答；C10 取消也留痕；C11 场景 S3 链条在流中 | ask-user 结构 + 固化 | FR-CHAT-040~044 / AC-CHAT-003 / AC-CHAT-015 |
| D（7） | D1 批准/拒绝 + 范围后果预演；D2 操作前；D3 批准后 `data-decision=approved`；D4 固化文案 + 时间戳 + 审计入口；D5 拒绝固化；D6 不可重复决策；D7 两态并存旧卡不消失 | 授权卡结构 + 两态固化 | FR-CHAT-045~047 / AC-CHAT-003 / AC-CHAT-016 |
| E（5） | E1 系统事件行 ≥3 条带时间戳；E2 引用失效事件行；E3 失效卡含原因 + 重拾 + 改用描述；E4 改用描述兜底默认收起；E5 失效旧卡保留 + 新引用序号递增 | 系统事件行 + 引用卡 | FR-CHAT-032 / FR-CHAT-050~052 / AC-CHAT-011 / AC-CHAT-012 |
| F（7） | F1 工具栏可点 ≤5；F2 工具栏 + 状态栏 ≤7；F3 无风险时状态栏收缩一行（0 chip）；F4 4 视图入口带计数徽标；F5 站点摘要只读不计入；F6 法一（工具栏/状态栏无一次性交互）；F7 法四（默认屏无常驻输入框） | 法一/四/五 + 工具栏准入 | FR-CHAT-011 / FR-CHAT-012 / FR-CHAT-014 / AC-CHAT-004 / AC-CHAT-007 / AC-CHAT-008 |
| G（7） | G1 三宽度切换器；G2 切 320；G3 切 520；G4 主题切换器（跟随/浅/深）；G5 主题按钮计入 ≤5；G6 深色真实生效；G7 切回跟随系统 | 窄屏 + 双主题 | NFR-CHAT-002 / NFR-CHAT-008 / AC-CHAT-022 |
| H（12） | H1 7 场景；H2 S2 引用+AI确认+推荐；H3 S5 系统事件+失效卡+新引用；H4 S6 风险稳态 2 chip 且 ≤7；H5 风险 chip 永不折叠；H6 风险详情默认收起；H7 chip 展开详情 + 披露语；H8 S6 状态栏可见；H9 L2 审计视图替换；H10 返回流恢复；H11 S7 7 类同屏；H12 S7 声明无效 + 引用失效并存 | 场景/风险 chips/视图替换 | FR-CHAT-013 / FR-CHAT-015 / FR-CHAT-017 / AC-CHAT-002 / AC-CHAT-007 / AC-CHAT-022 |
| I（1） | I1 切回 S1 后默认密度仍 ≤7 且脏数据复位 | 常态复位/幂等 | AC-CHAT-009 |

---

## 15. 修订记录

| 版本 | 变更说明 | 日期 | 修订人 |
|------|---------|------|--------|
| v1.0 | 初始创建（v4「聊天流统一承载」需求规范：父 Feature = 轻量规范容器 + 4 个叶子子 Feature）。立项来源（作者原话 5 条逐字）+ 编排器裁决承接 D1~D7 + **O-CHAT-001~009 逐条裁决记录**（含新增**法则六**「过程入流，管理入视图」/ **卡分类学 7 主类 + 过程卡族** / **密度新口径 + 两条防滥用** / **journey 保护段处置** / **V3-VOL-3 父级收口锚 + 绝对上限推导规则** / **O-CHAT-009 非阻塞**）+ 现状事实核对（全量 `file:line`）+ 目标/非目标（G-CHAT-001~007 / NG-CHAT-001~011）+ 用户故事 7 + **FR-CHAT-001~094（10 组：GOV/ZONE/STREAM/CARD/FLOW/TRACE/NEXT/DENSITY/LEDGER/VOL）** + NFR-CHAT-001~012 + EC-CHAT-001~014 + AC-CHAT-001~025 + 密度新口径 §9 + 取代台账与保护段 §10 + 叶子拆分与交付顺序 §11 + 附录 A（60 断言 → 条款映射） | 2026-09-18 | SDDU Spec Agent |
